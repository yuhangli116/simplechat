# SimpleChat 登录态全站功能测试验收方案

## 1. 验收目标

本轮验收以“登录状态下的真实用户路径”为主，逐页验证 SimpleChat 主要功能按钮是否可用、日志是否完整、数据是否安全落库、AI 调用是否正确扣费并进入消费流水。

验收优先级：

1. 高优先级：工作台思维导图、正文章节、我的作品导航栏、废稿箱、模版广场/指令工坊、奖励中心、消费明细。
2. 中优先级：使用指南内容正确性与教程入口。
3. 低优先级：充值中心。本轮只做入口与充值记录展示准备检查，不验收真实支付。

本方案先输出测试设计与执行标准。后续执行时必须一页一页验收，发现日志不足时先补日志，再重新通过浏览器操作复测。

## 2. 测试环境与账号

- 本地站点：优先使用当前浏览器正在访问的 `http://localhost:5174`。如同时存在 `5173`，需要确认当前测试 tab 所属 origin，浏览器缓存按 origin 隔离。
- 测试账号：使用用户提供的登录测试账号。密码不写入文档。
- 日志文件：`log/simplechat.log`
- 数据库：Supabase，登录态下通过 RLS 访问用户自己的数据。
- 测试数据命名建议：所有新增作品、章节、模板、指令统一前缀 `QA验收-YYYYMMDD-HHMM-功能名`，便于追踪与清理。

## 3. 总体执行规则

### 3.1 每个功能按钮的验收口径

每一个按钮至少需要验证以下 5 类证据：

1. UI 证据：点击后页面状态、弹窗、按钮状态、toast、内容变化符合预期。
2. 日志证据：`log/simplechat.log` 中有明确的开始、成功、失败日志，且包含关键字段。
3. 数据证据：涉及持久化的操作必须查对应表确认新增、更新、删除、恢复、扣费、流水正确。
4. 失败保护：取消、重复点击、无选择、锁定状态、网络失败、权限不足等边界行为不破坏数据。
5. 可恢复性：删除类操作进入废稿箱或有二次确认；不可恢复操作必须确认提示明确。

### 3.2 日志不足时的处理规则

如果按钮功能本身可用，但日志不足，不直接通过验收。流程如下：

1. 标记为 `阻塞：日志不足`。
2. 补充最小必要日志：
   - 操作开始：`log.info`
   - 操作成功：`log.success`
   - 操作失败：`log.error`
   - 可恢复/降级：`log.warn`
3. 日志字段必须包含：`userId`、业务对象 ID、对象类型、操作名、前后数量或关键状态、错误信息。
4. 补日志后重新浏览器操作验证，并确认日志已刷入 `log/simplechat.log`。

### 3.3 数据安全规则

- 不使用用户真实重要作品做删除/清空测试。先创建测试作品/章节/模板/指令，再对测试数据操作。
- 清空废稿箱只允许在确认废稿箱中全是 QA 测试数据时执行；否则改为单项或批量选择测试数据删除。
- AI 扣费类测试执行前后记录余额、`usage_logs`、页面“消费明细”展示，确认账实一致。
- 模板、指令、作品、章节、导图删除后，必须验证废稿箱记录和恢复路径。

## 4. 数据表与关键日志基线

### 4.1 主要落库表

- 作品树：`works`、`chapters`、`mind_maps`
- 章节版本：`chapter_versions`
- 废稿箱：`trash_items`
- 用户指令：`user_prompts`
- 社区作品模板：`community_templates`
- 作品模板点赞/收藏：`template_likes`、`user_templates`
- 社区提示词模板：`community_skill_templates`
- 提示词模板点赞/收藏：`skill_template_likes`、`user_skill_templates`
- 福利：`user_welfare`、`profiles`
- 消费流水：`usage_logs`
- 充值流水：`recharge_logs`
- 用户余额：`profiles.member_diamonds`、`profiles.permanent_diamonds`

### 4.2 AI 扣费场景

必须覆盖并查 `usage_logs`：

1. 思维导图 AI 智能生成节点：`billing_step = generate`；如参考上下文超过 3000 字，还应出现摘要扣费记录。
2. 正文参考大纲摘要：`billing_step = summarize`，缓存命中时不应新增摘要扣费。
3. 正文 AI 续写：`billing_step = generate`。
4. 组合参考大纲二次摘要：`billing_step = summarize`，同一 `billing_group_id` 下可能和 generate 形成一组。

校验字段：

- `billing_group_id`
- `billing_step`
- `model_name`
- `input_tokens`
- `output_tokens`
- `reasoning_tokens`
- `cache_tokens`
- `diamonds_consumed`
- `input_diamonds`
- `output_diamonds`
- `reasoning_diamonds`
- `cache_diamonds`
- `member_diamonds_used`
- `permanent_diamonds_used`
- `expires_at`

### 4.3 目前已知日志缺口，需要执行中确认或补齐

以下为代码静态检查发现的高概率日志不足点，执行时重点验证：

- 思维导图：放大、缩小、左对齐、右对齐、适应画布、锁住/解锁、主题切换、导出、撤销/恢复可能缺少明确操作日志。
- 思维导图 AI 生成：已有 AI 服务与部分保存日志，但建议补充 `Mind map AI generation started/succeeded/failed`，包含参考大纲数量、是否触发摘要、生成节点数量、`billingGroupId`。
- 正文章节：模型切换、参考大纲弹窗打开/确认、导出格式选择可能缺少专门日志。
- 我的作品导航栏：打包作品、编辑作品、删除页面/作品已有部分日志，仍需确认“打包”是否有开始/成功/失败日志。
- 废稿箱：提示词恢复有日志；作品/章节/导图恢复建议补充统一成功日志和落库对象 ID。
- 模版广场：创建/编辑/删除作品模板、创建/编辑/删除提示词模板目前以 alert 为主，需确认并补充日志。
- 福利中心：签到、任务领取、视频任务大多缺少 logger 日志，需要补充。
- 使用指南：打开视频/图文教程入口缺少日志，可以低优先补。
- 指令工坊：复制操作只有 toast，建议补充日志；新增/导入重名校验目前需要优化。

## 5. 逐页验收方案

## 5.1 工作台 - 思维导图页面

适用页面：

- `/workspace/p/:workId/outline`
- `/workspace/p/:workId/world`
- `/workspace/p/:workId/characters`
- `/workspace/p/:workId/events`
- `/workspace/p/:workId/mindmap/:mindMapId`

前置数据：

- 创建一个 QA 测试作品。
- 至少准备一个默认大纲和一个自定义大纲。
- 根节点下至少有 2 个子节点，便于兄弟节点、删除、批量删除、撤销恢复测试。

### 5.1.1 创建子节点

步骤：

1. 选中非锁定状态下的根节点或普通节点。
2. 点击“新增子节点”按钮。
3. 修改新节点标题。

预期 UI：

- 新节点出现在选中节点下方。
- 连线正确，节点布局不重叠。
- 保存状态最终变为已保存。

预期日志：

- `Mind map node added`
- `Mind map saved locally`
- `Saving mind map to remote` 或 `Mind map saved to remote`

预期落库：

- `mind_maps.content.nodes` 增加新节点。
- `mind_maps.content.edges` 增加对应边。

### 5.1.2 创建兄弟节点

步骤：

1. 选中非根节点。
2. 点击“新增兄弟节点”。
3. 尝试选中根节点后点击“新增兄弟节点”。

预期 UI：

- 普通节点成功创建兄弟节点。
- 根节点创建兄弟节点时提示“根节点无法添加兄弟节点”。

预期日志：

- 成功路径同 `Mind map node added`，`relation=sibling`。
- 根节点失败建议补日志：`Mind map add sibling blocked for root`。

### 5.1.3 放大、缩小、左对齐、右对齐、适应画布

步骤：

1. 点击放大、缩小，观察画布比例变化。
2. 点击左对齐、右对齐，观察视口平移。
3. 点击适应画布，所有节点进入可视区域。

预期日志：

- 如缺少，补充：
  - `Mind map zoom changed`
  - `Mind map viewport aligned`
  - `Mind map fit view requested`

落库要求：

- 这些视图操作不应改变 `mind_maps.content.nodes/edges`。

### 5.1.4 删除节点与批量删除节点

步骤：

1. 删除单个普通节点。
2. 删除含子节点的节点，确认二次确认文案。
3. 右键框选多个节点后批量删除。
4. 尝试删除根节点。

预期 UI：

- 普通节点及其子树被删除。
- 批量删除数量提示正确。
- 根节点不能删除。

预期日志：

- `Mind map node deleted`，字段包含 `deletedCount`、`nodeCount`、`edgeCount`。
- 根节点删除阻止建议补 `Mind map delete blocked for root`。

预期落库：

- `mind_maps.content.nodes/edges` 删除对应项。

### 5.1.5 AI 智能生成节点（重要，含参考大纲）

步骤：

1. 选中一个节点。
2. 点击“AI 智能生成”。
3. 在弹窗中输入明确需求。
4. 添加一个短参考大纲，验证不触发摘要。
5. 添加一个超过 800 字的参考内容，验证触发摘要缓存。
6. 再次使用相同参考，验证命中缓存不重复扣参考摘要费。
7. 确认 AI 生成节点被插入并保存。

预期 UI：

- AI 弹窗可打开、可选择参考、可生成。
- 生成中按钮禁用或显示 loading。
- 生成成功后节点和子节点插入当前导图。

预期日志：

- `Generate text started/completed`
- `AI task billing completed`
- `Mind map node added` 或建议新增 `Mind map AI generation completed`
- 如果参考超过 3000 字：
  - `Summarize context started/completed`
  - `billingStep=summarize`

预期落库：

- `mind_maps` 内容更新。
- `usage_logs` 有 AI 生成消费记录。
- 余额减少值与 `usage_logs.diamonds_consumed` 一致。
- 消费明细页面展示该记录。

### 5.1.6 撤销、恢复

步骤：

1. 新增节点后点击“向左撤销”。
2. 再点击“向右恢复”。
3. 连续撤销超过历史容量，验证提示与数据保护。

预期 UI：

- 撤销恢复节点状态正确。
- 超范围撤销出现确认提示。

预期日志：

- 如缺少，补充：
  - `Mind map undo requested/succeeded`
  - `Mind map redo requested/succeeded`
  - `Mind map undo clear-all confirmation shown`

### 5.1.7 锁住/解锁页面

步骤：

1. 点击锁住页面。
2. 尝试选择节点、编辑节点、添加节点、删除节点。
3. 点击解锁页面，重复操作。

预期 UI：

- 锁定状态禁止编辑，出现提示。
- 解锁后恢复可编辑。

预期日志：

- 如缺少，补充 `Mind map lock toggled`，字段包含 `locked`。

### 5.1.8 下载、切换主题

步骤：

1. 点击下载，选择图片/JSON/文本等格式。
2. 点击主题按钮，切换所有主题。

预期 UI：

- 下载文件成功生成。
- 主题即时生效，并刷新后保持。

预期日志：

- 如缺少，补充：
  - `Mind map export requested/completed`
  - `Mind map theme changed`

预期落库：

- 主题通常存储在 `localStorage`，不应污染远端 mind map 内容。

## 5.2 工作台 - 正文章节页面

页面：`/workspace/p/:workId/story/:chapterId`

前置数据：

- 至少 2 个 QA 章节：空正文、有正文。
- 至少 2 个参考：短参考、小于 800 字；长参考，大于 800 字。

### 5.2.1 选择模型

步骤：

1. 打开模型选择弹窗。
2. 切换到非当前模型。
3. 发起一次 AI 续写。

预期 UI：

- 顶部模型名称更新。
- AI 调用使用所选模型。

预期日志：

- 建议补充 `Story model selected`。
- AI 服务日志中 `model` 与 UI 一致。

### 5.2.2 参考大纲（重要）

步骤：

1. 点击“参考大纲”。
2. 选择短参考，确认提示“不需要单独总结”。
3. 选择长参考，确认触发摘要、写缓存。
4. 删除参考标签。
5. 重复添加同一参考，验证重复选择保护。

预期日志：

- `AI reference pre-summary skipped: content below node threshold`
- `AI reference summary started; billing will be charged`
- `AI reference summary completed and cached`
- `AI reference summary reused without billing`
- `AI reference context selection ignored because it already exists`

预期落库与缓存：

- 参考摘要缓存存储在用户浏览器 `localStorage`，key 格式为 `story-ai-summary-v2-${userId}-${contentHash}`，7 天过期。
- 缓存不落服务端。
- 长参考首次摘要需要 `usage_logs.billing_step=summarize`。
- 同内容二次复用不新增 summarize 账单。

### 5.2.3 AI 续写（重要）与继续应用

步骤：

1. 空正文 + 无参考。
2. 空正文 + 有参考。
3. 已有正文 + 无参考。
4. 已有正文 + 有参考。
5. 验证 AI 输出先展示给用户，后台继续扣费/保存。
6. 点击“取消应用”。
7. 再次生成并点击“继续应用”。

预期 UI：

- 输出流式展示。
- AI 输出完成后出现对比/应用面板。
- “继续应用”后内容追加或替换策略符合预期。
- 应用后状态变为已保存。

预期日志：

- `AI continue started`
- `AI continue UX mode selected`
- `AI continue first delta rendered`
- `AI continue marked complete for user; background billing/save continues`
- `AI continue background phase hidden from UI`
- `Pending AI version comparison opened`
- `Applying pending AI version`
- `Pending AI version applied`
- `Chapter content auto-saved`

预期落库：

- `usage_logs` 写入 generate 消费。
- `chapters.content` 更新。
- `chapter_versions` 创建 `ai-current-before-replace` 与 `ai-applied`。
- `profiles` 余额减少等于流水汇总。

### 5.2.4 历史版本（重要）

步骤：

1. AI 应用后打开历史版本。
2. 选择一个历史版本预览。
3. 点击恢复。
4. 恢复前后再次打开历史版本。

预期 UI：

- 版本列表显示来源、时间、内容预览。
- 恢复后正文变为所选版本。

预期日志：

- `Chapter version history opened`
- `Chapter version restored`
- `Chapter version snapshot created`

预期落库：

- `chapter_versions` 有恢复前快照。
- `chapters.content` 更新为恢复版本。

### 5.2.5 保存、下载/另存为、编辑器工具栏

步骤：

1. 修改正文点击保存。
2. 使用加粗、颜色、删除线、标题、对齐、列表。
3. 点击另存为，分别导出 HTML/Markdown/TXT。

预期日志：

- `Manual chapter save requested/completed`
- `Chapter export requested/completed`

预期落库：

- `chapters.content` 与页面 HTML 一致。

## 5.3 工作台 - 我的作品导航栏页面

覆盖区域：左侧“我的作品”树。

### 5.3.1 新增作品

步骤：

1. 点击“新建作品”。
2. 输入 QA 作品名。
3. 确认创建。

预期日志：

- `Creating work`
- `Work created and persisted`
- `Workspace tree loaded/refreshed`

预期落库：

- `works` 新增。
- 默认 `mind_maps` 和章节结构按产品预期生成。

### 5.3.2 编辑作品/页面名称

步骤：

1. 点击作品或页面的编辑按钮。
2. 修改名称并保存。

预期日志：

- `Renaming workspace node`
- `Workspace node renamed`
- `Workspace tree change persisted`

预期落库：

- 作品名更新到 `works.title`。
- 章节名更新到 `chapters.title`。
- 导图名更新到 `mind_maps.title`。

### 5.3.3 新增页面/章节/自定义大纲

步骤：

1. 对作品点击新增章节。
2. 对作品相关点击新增自定义大纲。
3. 打开新增页面并编辑保存。

预期日志：

- `Creating chapter`
- `Chapter created`
- `Creating custom mind map`
- `Custom mind map created`

预期落库：

- `chapters` 或 `mind_maps` 新增。

### 5.3.4 删除作品、删除页面

步骤：

1. 删除 QA 章节。
2. 删除 QA 自定义导图。
3. 删除 QA 作品。

预期 UI：

- 二次确认。
- 删除后从导航树消失。

预期日志：

- `Deleting workspace node to trash`
- `Trying atomic workspace trash RPC`
- `Atomic workspace trash RPC completed` 或 fallback 成功日志
- `Workspace node moved to trash`

预期落库：

- 原表数据删除或按设计移动。
- `trash_items` 新增记录，含完整快照。

### 5.3.5 打包作品

步骤：

1. 点击作品“打包”。
2. 下载 ZIP 或导出包。
3. 解压检查章节、导图、元信息完整。

预期日志：

- 若缺少，补充 `Work package export requested/completed/failed`。

## 5.4 废稿箱页面

页面：`/trash`

前置数据：

- 通过删除 QA 作品、QA 章节、QA 导图、QA 指令生成废稿箱数据。

### 5.4.1 筛选与展示

步骤：

1. 切换全部、作品、提示词筛选。
2. 验证删除时间、类型、标题、过期提示。

预期日志：

- `Syncing trash items from Supabase`
- `Trash items synced from Supabase`

### 5.4.2 恢复某个页面/提示词/作品

步骤：

1. 单项恢复 QA 章节。
2. 单项恢复 QA 指令。
3. 恢复整个 QA 作品。

预期日志：

- 提示词已有 `Prompt restored from trash to database`。
- 作品/章节/导图恢复如缺少，补充 `Trash item restored`，字段包含 `trashId`、`type`、`restoredId`。

预期落库：

- `trash_items` 对应项删除。
- 对应业务表恢复数据。

### 5.4.3 恢复选中、删除选中、清空废稿箱

步骤：

1. 勾选多个 QA 项，点击恢复选中。
2. 勾选多个 QA 项，点击彻底删除。
3. 在废稿箱只有 QA 数据时点击清空。

预期 UI：

- 有二次确认。
- 部分失败时不应删除失败项。

预期日志：

- `Permanently deleting trash item`
- `Trash item removed from local store`
- `Clearing all trash items`
- `All trash items cleared locally`
- 批量恢复建议补充 `Trash batch restore completed`。

预期落库：

- 彻底删除后 `trash_items` 不存在对应记录。
- 清空只影响当前用户数据。

## 5.5 模版广场与社区指令模板

页面：`/community`

### 5.5.1 作品模板列表：浏览、点赞、收藏、使用模板

步骤：

1. 打开作品模板 tab。
2. 点击模板卡片进入预览，确认浏览数变化。
3. 点赞，再取消点赞。
4. 收藏，再取消收藏。
5. 点击使用模板，生成一个新作品。

预期日志：

- `Opening template preview`
- `Toggling template like`
- `Template liked/unliked`
- `Toggling template collect`
- `Template collected/uncollected`
- `Use template requested`
- `Template applied to workspace`
- `Template work persisted for logged-in user`

预期落库：

- `community_templates.views/downloads` 按设计递增。
- `template_likes` 插入/删除。
- `user_templates` 插入/删除。
- 使用模板后 `works`、`chapters`、`mind_maps` 创建完整。

### 5.5.2 创建作品模板（重要）

步骤：

1. 点击创建模板。
2. 创建作品模板，填写名称、说明、分类、节点结构。
3. 进入我的模板验证展示。
4. 编辑模板。
5. 删除模板。

预期日志：

- 目前可能不足，需补：
  - `Community work template create requested/succeeded/failed`
  - `Community work template update requested/succeeded/failed`
  - `Community work template delete requested/succeeded/failed`

预期落库：

- `community_templates` 新增/更新/删除。
- `creator_id = 当前用户`。
- 非官方模板上限 10 个。

### 5.5.3 提示词模板：复制、导入、点赞、收藏、浏览

步骤：

1. 打开提示词模板 tab。
2. 点击预览，确认 uses 或浏览数递增。
3. 点击复制，确认剪贴板内容。
4. 点击导入，进入指令工坊确认新增。
5. 点赞、收藏、取消收藏。

预期日志：

- `Opening skill preview`
- `Toggling skill template like`
- `Skill template liked/unliked`
- `Toggling collected skill template`
- `Skill template collected/uncollected`
- `Failed to import skill to prompts` 仅失败时出现；成功路径建议补 `Skill imported to prompts`。

预期落库：

- `skill_template_likes` 插入/删除。
- `user_skill_templates` 插入/删除。
- `community_skill_templates.uses/likes` 更新。
- `user_prompts` 新增或更新。

### 5.5.4 我的模板与收藏列表

步骤：

1. 切换我的模板：作品模板、提示词模板。
2. 对自己创建的模板执行编辑、删除、使用。
3. 切换收藏列表：作品模板、提示词模板。
4. 取消收藏。

预期：

- 只允许编辑/删除自己创建的模板。
- 官方模板不能编辑/删除。
- 取消收藏后列表即时移除，数据库关联删除。

## 5.6 奖励中心页面

页面：`/welfare`

### 5.6.1 新手立即签到（重要）

步骤：

1. 记录签到前 `profiles` 余额和 `user_welfare` 状态。
2. 点击“立即签到”。
3. 再次点击，验证不可重复领取。

预期 UI：

- 成功 toast 显示奖励钻石。
- 今日状态变为已签到。

预期日志：

- 目前缺少明显 logger 日志，需补：
  - `Welfare check-in requested`
  - `Welfare check-in succeeded`
  - `Welfare check-in failed`

预期落库：

- `user_welfare.last_check_in_date = 今日`
- `user_welfare.check_in_streak` 更新。
- `profiles.permanent_diamonds` 或实际奖励余额字段增加。

### 5.6.2 去充值

步骤：

1. 点击“去充值”。

预期：

- 跳转 `/membership`。
- 本轮不验收真实支付。
- 建议补日志 `Welfare recharge navigation clicked`。

### 5.6.3 三个任务按钮

任务：

- 完成首次 AI 生成
- 完成首次创建模板
- 观看激励视频

步骤：

1. 点击首次 AI 生成任务，确认跳转到工作台。
2. 完成一次 AI 生成后回到奖励中心领取。
3. 点击首次创建模板任务，跳转社区并创建模板后领取。
4. 点击观看激励视频，验证不能快进，80% 后可领取。

预期日志：

- 需补：
  - `Welfare task navigation clicked`
  - `Welfare task claim requested/succeeded/failed`
  - `Welfare video opened/progress/claim`

预期落库：

- `user_welfare.completed_tasks` 增加任务 key。
- `profiles` 余额增加。

## 5.7 使用指南页面

页面：`/guide`

优先级低，但要检查入口有效。

步骤：

1. 切换视频教程 tab。
2. 打开每个视频教程，确认弹窗、视频源、关闭。
3. 切换图文教程 tab。
4. 打开每个图文教程，确认内容与最新功能一致。

需要优化的图文内容：

- 参考大纲缓存：超过 800 字参考会先摘要并缓存 7 天，缓存保存在本机浏览器。
- 正文 AI 续写：AI 输出先预览，点击继续应用后保存，历史版本可恢复。
- 思维导图 AI 生成：可添加参考上下文，大纲生成会扣钻石，消费明细可查。
- 废稿箱：作品/章节/导图/指令删除进入废稿箱，彻底删除不可恢复。
- 指令工坊：新增/导入指令不允许重名。
- 消费明细：AI 参考摘要、AI 续写、AI 生成节点会形成消费流水。

预期日志：

- 可低优先补：
  - `Guide tab changed`
  - `Guide item opened`

## 5.8 指令工坊页面

页面：`/prompts`

### 5.8.1 新增指令

步骤：

1. 点击新增指令。
2. 填写标题、一级标签、标签、内容。
3. 保存。

预期日志：

- `Creating user prompt`
- `User prompt created`
- `Prompt saved from Prompts page`

预期落库：

- `user_prompts` 新增。

### 5.8.2 重名校验（需要优化）

当前风险：

- 新增指令时未看到基于标题的重名拦截。
- 从社区导入提示词模板时当前逻辑主要按 `sourceSkillTemplateId` 查重；用户手动新增同名指令或不同来源同名模板可能重复。

优化要求：

1. 新增指令时，同一用户下 `title.trim()` 不允许重复，编辑自身除外。
2. 导入指令时，同一用户下如已有同名标题：
   - 如果来源相同，可提示“已导入过，是否覆盖更新”。
   - 如果来源不同但标题相同，应提示重名，不允许直接新增；可让用户改名后导入。
3. 数据库建议增加唯一约束或应用层强校验：
   - 应用层：保存前检查 `prompts` store。
   - 服务端/DB：可评估 `unique(user_id, lower(title))`，注意 Supabase/Postgres 表达式索引迁移。

验收步骤：

1. 新增 `QA重复指令`。
2. 再新增同名指令，预期阻止。
3. 从社区导入同名指令，预期阻止或覆盖确认符合设计。

预期日志：

- 新增 `Prompt duplicate blocked`。

### 5.8.3 复制、编辑、删除

步骤：

1. 复制指令。
2. 编辑标题/内容保存。
3. 删除指令。
4. 到废稿箱恢复该指令。

预期日志：

- 复制建议补 `Prompt copied`。
- 编辑已有 `Updating user prompt`、`User prompt updated`。
- 删除进入废稿箱：`Adding item to trash`、`User prompt deleted`。

预期落库：

- `user_prompts` 更新/删除。
- `trash_items` 新增。
- 恢复后 `user_prompts` 恢复。

## 5.9 充值中心页面

页面：`/membership`

本轮低优先级，不验收真实支付。

只做：

1. 页面能打开。
2. 会员/加油包展示正确。
3. 模拟支付如仍存在，需要标记为非生产验收功能。
4. 不做真实扣款，不作为上线支付验收依据。

后续真实支付上线后，需要另起支付专项验收。

## 5.10 消费明细页面

页面：`/records`

### 5.10.1 消费记录

步骤：

1. 执行一次思维导图 AI 生成节点。
2. 执行一次正文参考大纲摘要。
3. 执行一次正文 AI 续写。
4. 打开消费明细。
5. 切换分页，查看最新记录。

预期 UI：

- 消费记录展示模型、tokens、钻石消耗、会员/永久钻石拆分、过期时间。
- 同一 `billing_group_id` 如果有 summarize + generate，应聚合展示或明细清晰。

预期日志：

- `fetchUsage called`
- `AI task billing completed`
- `Deduct diamonds success`

预期落库：

- `usage_logs` 有上述三类记录。
- 页面展示值等于数据库值。
- `profiles.member_diamonds + profiles.permanent_diamonds` 变化等于流水扣减。

### 5.10.2 充值记录

步骤：

1. 打开充值记录 tab。
2. 检查已有记录展示。

预期日志：

- `fetchRecharge called`

预期落库：

- `recharge_logs` 只展示当前用户自己的记录。

### 5.10.3 流水准确性专项

每次 AI 调用记录：

1. 调用前余额 A。
2. 调用后余额 B。
3. 查询最新 `usage_logs` 汇总 C。
4. 断言 `A - B = C`。
5. 如前端消费明细聚合，同一 `billing_group_id` 汇总值必须等于 C。

缓存场景断言：

- 参考摘要首次生成：有 summarize 账单。
- 相同参考再次使用：无新增 summarize 账单。
- AI 续写本体仍有 generate 账单。

## 6. 浏览器验收执行顺序建议

1. 登录测试账号，确认顶部用户与余额。
2. 创建 QA 作品和基础章节/导图。
3. 工作台导航栏：新增、编辑、删除、废稿箱链路。
4. 思维导图：普通按钮、锁定、主题、下载、AI 生成。
5. 正文章节：模型、参考、AI 续写、历史版本、保存、导出。
6. 消费明细：先核对 AI 流水，避免后续测试数据混淆。
7. 模版广场：作品模板、提示词模板、我的模板、收藏列表。
8. 指令工坊：新增/编辑/复制/删除/恢复，重名校验。
9. 奖励中心：签到、任务、视频。
10. 废稿箱：批量恢复/删除/清空，仅限 QA 数据。
11. 使用指南：低优先入口与内容检查。

## 7. 验收记录模板

每个按钮验收时记录如下：

```text
页面：
功能按钮：
测试数据：
操作步骤：
UI 结果：
日志关键行：
数据库核对：
消费/余额核对：
结论：通过 / 失败 / 阻塞-日志不足 / 阻塞-需修复
问题编号：
修复建议：
复测结果：
```

## 8. 首轮重点风险清单

1. 指令新增/导入重名校验缺失，需优先修复。
2. 福利中心关键操作缺少业务日志。
3. 社区模板创建/编辑/删除成功路径日志不足。
4. 思维导图多个工具按钮缺少日志，影响“结合日志验收”。
5. AI 扣费必须重点防重扣，尤其是参考摘要缓存命中、billing RPC 重试、同一 `billing_group_id` 幂等。
6. 废稿箱恢复作品/导图/章节需要重点验证快照完整性，不能恢复失败却删除废稿箱记录。
7. 使用指南内容需要同步最新功能：参考大纲 800 字阈值、7 天浏览器缓存、AI 预览后应用、指令重名规则。

## 9. 本方案产出后的下一步

1. 按本方案先补齐必要日志缺口。
2. 用浏览器逐页执行验收。
3. 每完成一个页面，追加实际测试记录和问题清单。
4. 对发现的问题逐个修复，修复后按同一用例复测。
5. 最终形成完整验收报告，包含通过项、修复项、未覆盖项、残余风险。

## 10. 验收前准入检查

正式开始浏览器验收前，必须先完成以下准入项。任一高优先级准入项失败，本轮不进入全量按钮验收，先修复基础能力。

### 10.1 服务与登录态

检查项：

1. `http://localhost:5174` 可访问；如果实际测试使用 `5173`，所有缓存、登录态、日志都按 `5173` 单独记录。
2. 登录测试账号后，顶部用户信息和钻石余额展示正常。
3. 刷新当前工作台页面后仍保持登录态。
4. 访问 `/records`、`/welfare`、`/community`、`/prompts` 均不跳回登录页。
5. `log/simplechat.log` 能持续写入前端 `/api/log` 和服务端日志。

通过标准：

- 页面可用、登录态稳定、日志文件能看到本次操作。
- 如果日志不写入，先修复日志接口或前端 `flushLogs` 触发问题。

### 10.2 测试批次号

本轮所有测试数据使用统一批次号：

```text
QA验收-20260627-登录态全站
```

如果实际执行时间变化，批次号格式保持：

```text
QA验收-YYYYMMDD-HHMM-功能名
```

建议测试对象命名：

- 作品：`QA验收-20260627-主作品`
- 章节：`QA验收-空正文`、`QA验收-已有正文`、`QA验收-AI续写`
- 自定义大纲：`QA验收-自定义导图`
- 指令：`QA验收-重复指令`
- 作品模板：`QA验收-作品模板`
- 提示词模板：`QA验收-提示词模板`

清理规则：

- 验收中不要删除非 QA 前缀的数据。
- 需要清空废稿箱时，必须先确认废稿箱里只有 QA 前缀数据；否则只删除选中的 QA 项。
- AI 消费流水不删除，只通过批次时间和 `billing_group_id` 记录核账。

### 10.3 日志基准时间

开始每个页面验收前记录一个基准时间：

```text
页面：
开始时间：
结束时间：
测试账号 userId：
测试 workId/chapterId/mindMapId：
```

日志检索时只看开始时间之后的日志，避免被历史测试污染。

建议检索方式：

```bash
rg "AI reference|AI task billing|Deduct diamonds|Mind map|Prompt|Trash|Welfare|Community|fetchUsage" log/simplechat.log
```

如果某个动作没有日志，先补日志再复测，不用反复点击同一个按钮。

## 11. 测试数据准备方案

### 11.1 工作台测试数据

创建一个主 QA 作品，作品内准备以下数据：

| 数据 | 用途 | 必要内容 |
| --- | --- | --- |
| `QA验收-空正文` | 验证空正文 AI 续写 | 正文为空 |
| `QA验收-已有正文` | 验证已有正文 AI 续写 | 至少 300 字正文 |
| `QA验收-短参考` | 参考大纲不摘要场景 | 300-799 字 |
| `QA验收-长参考800` | 单节点参考摘要阈值 | 800-2999 字 |
| `QA验收-超长参考3000` | 组合参考摘要与扣费 | 3000 字以上 |
| `QA验收-历史版本` | 验证版本恢复 | 至少保存 2 次 |

思维导图准备：

- 根节点 1 个。
- 子节点不少于 3 个。
- 至少一个节点有 2 层子节点，用于删除子树。
- 至少一个节点标题包含长文本，用于布局与导出观察。

### 11.2 社区与指令测试数据

社区模板准备：

- 创建 1 个作品模板，包含至少 1 个章节、1 个默认大纲、1 个自定义导图。
- 创建 1 个提示词模板，标题与指令工坊中 `QA验收-重复指令` 相同，用于重名导入校验。
- 收藏 1 个官方或其他模板，用于取消收藏验证。

指令工坊准备：

- 新增 `QA验收-重复指令`。
- 再次新增同名，用于验证阻止。
- 从社区导入同名提示词模板，用于验证导入阻止或覆盖确认。

废稿箱准备：

- 删除 1 个 QA 章节。
- 删除 1 个 QA 自定义导图。
- 删除 1 个 QA 作品。
- 删除 1 个 QA 指令。

### 11.3 AI 扣费基线数据

每个 AI 测试开始前记录：

```text
AI 测试编号：
开始时间：
用户总余额 before = member_diamonds + permanent_diamonds：
member_diamonds before：
permanent_diamonds before：
usage_logs 最新 created_at：
```

结束后记录：

```text
用户总余额 after：
member_diamonds after：
permanent_diamonds after：
新增 usage_logs：
新增 billing_group_id：
新增 billing_step：
流水 diamonds_consumed 汇总：
断言 before - after = diamonds_consumed 汇总：
```

## 12. 日志补齐实施清单

本节是验收前或验收中发现缺口时的最小补日志清单。日志不要求华丽，但必须能支持追责、复测和核账。

### 12.1 通用字段规范

所有业务日志尽量包含：

- `userId`
- `route`
- `workId`
- `chapterId`
- `mindMapId`
- `objectId`
- `objectType`
- `action`
- `status`
- `durationMs`
- `error`

AI 和扣费日志额外包含：

- `traceId`
- `billingGroupId`
- `billingStep`
- `model`
- `inputTokens`
- `outputTokens`
- `reasoningTokens`
- `cacheTokens`
- `diamondsConsumed`
- `balanceBefore`
- `balanceAfter`

缓存日志额外包含：

- `cacheKey`
- `contentHash`
- `sourceName`
- `originalLength`
- `summaryLength`
- `ttlMs`
- `ageMs`
- `cacheHit`
- `cacheLocation=localStorage`

### 12.2 思维导图日志

必须补齐或确认存在：

| 操作 | 开始日志 | 成功日志 | 失败/阻止日志 |
| --- | --- | --- | --- |
| 创建子节点 | `Mind map add child requested` | `Mind map add child succeeded` | `Mind map add child failed` |
| 创建兄弟节点 | `Mind map add sibling requested` | `Mind map add sibling succeeded` | `Mind map add sibling blocked` |
| 删除节点 | `Mind map delete requested` | `Mind map delete succeeded` | `Mind map delete blocked` |
| 批量删除 | `Mind map batch delete requested` | `Mind map batch delete succeeded` | `Mind map batch delete failed` |
| AI 生成节点 | `Mind map AI generation requested` | `Mind map AI generation succeeded` | `Mind map AI generation failed` |
| 缩放 | `Mind map zoom changed` | 同一条即可 | `Mind map zoom failed` |
| 对齐/适应 | `Mind map viewport action requested` | `Mind map viewport action succeeded` | `Mind map viewport action failed` |
| 锁定/解锁 | `Mind map lock toggled` | 同一条即可 | `Mind map lock toggle failed` |
| 下载 | `Mind map export requested` | `Mind map export succeeded` | `Mind map export failed` |
| 主题 | `Mind map theme changed` | 同一条即可 | `Mind map theme change failed` |

### 12.3 正文章节日志

必须补齐或确认存在：

| 操作 | 必要日志 |
| --- | --- |
| 切换模型 | `Story model selected` |
| 打开参考大纲 | `Story reference dialog opened` |
| 短参考跳过摘要 | `AI reference pre-summary skipped: content below node threshold` |
| 长参考摘要开始 | `AI reference summary started; billing will be charged` |
| 缓存命中 | `AI reference summary reused without billing` 或 `AI reference summary cache hit` |
| 缓存写入 | `AI reference summary cached` |
| AI 续写开始 | `AI continue started` |
| 首 token 展示 | `AI continue first delta rendered` |
| 用户可见完成 | `AI continue marked complete for user` |
| 后台扣费保存 | `AI continue background billing/save completed` |
| 应用版本 | `Pending AI version applied` |
| 历史版本打开 | `Chapter version history opened` |
| 历史版本恢复 | `Chapter version restored` |
| 手动保存 | `Manual chapter save requested/completed` |
| 下载 | `Chapter export requested/completed` |

### 12.4 社区、指令、福利日志

社区必须补齐：

- `Community work template create requested/succeeded/failed`
- `Community work template update requested/succeeded/failed`
- `Community work template delete requested/succeeded/failed`
- `Community skill template create requested/succeeded/failed`
- `Community skill template update requested/succeeded/failed`
- `Community skill template delete requested/succeeded/failed`
- `Skill imported to prompts`
- `Skill import duplicate title blocked`
- `Template preview opened`
- `Skill preview opened`

指令工坊必须补齐：

- `Prompt duplicate blocked`
- `Prompt copied`
- `Prompt preview opened`
- `Prompt delete confirmed`

福利中心必须补齐：

- `Welfare data loaded`
- `Welfare check-in requested/succeeded/failed`
- `Welfare task navigation clicked`
- `Welfare task claim requested/succeeded/failed`
- `Welfare video opened`
- `Welfare video seek blocked`
- `Welfare video progress reached`
- `Welfare video reward claimed`

## 13. 参考大纲缓存专项验收

这是本轮已做改造后的重点专项，必须单独验收。

### 13.1 当前设计结论

当前参考大纲摘要缓存为浏览器侧缓存：

- 存储位置：用户自己的浏览器 `localStorage`。
- key 前缀：`story-ai-summary-v2`。
- key 结构：`story-ai-summary-v2-${userId}-${contentHash}`。
- 过期时间：7 天。
- 最大条数：20 条。
- 最大总量：500 KB。
- 隔离维度：用户 + 参考内容 hash。
- 已去掉模型维度：切换模型后相同参考内容仍可复用摘要缓存。
- 不再兼容读取旧 key：旧测试缓存清理后，新缓存按 v2 重新生成。

注意：

- 缓存 entry 里仍保留 `model` 字段作为生成来源记录，但不参与 key 隔离。
- 缓存只缓存“参考摘要”，不缓存 AI 续写正文结果。
- 浏览器换设备、换浏览器、清理站点数据后缓存消失。
- 服务端不持有这份参考摘要缓存，因此不会在不同用户或不同设备间共享。

### 13.2 缓存阈值验收

用例 `CACHE-001`：短参考不摘要

1. 选择 799 字以内参考。
2. 点击确认参考。
3. 发起 AI 续写。

预期：

- 不写入 `story-ai-summary-v2` 缓存。
- 不产生 `billing_step=summarize` 的 `usage_logs`。
- 日志出现“低于阈值跳过摘要”。

用例 `CACHE-002`：800 字及以上单节点参考进入摘要策略

1. 选择 800 字以上参考。
2. 确认参考。
3. 首次触发摘要。

预期：

- `localStorage` 生成 `story-ai-summary-v2-${userId}-${contentHash}`。
- 日志出现摘要开始、摘要完成、缓存写入。
- `usage_logs` 新增 `billing_step=summarize`。

用例 `CACHE-003`：同参考二次使用命中缓存

1. 保持同一用户、同一浏览器、同一 origin。
2. 再次选择同一参考内容。
3. 发起同类 AI 续写。

预期：

- 日志出现 cache hit 或 reused without billing。
- 不新增 `billing_step=summarize` 流水。
- AI 续写本身仍应新增 `billing_step=generate` 流水。

用例 `CACHE-004`：切换模型仍命中缓存

1. 首次用模型 A 生成参考摘要。
2. 切换到模型 B。
3. 使用相同参考。

预期：

- 仍命中同一个 key。
- 不因模型变化重新摘要扣费。
- `generate` 流水中的模型应为模型 B。

用例 `CACHE-005`：不同内容不误命中

1. 修改参考内容 1 个字符。
2. 重新选择参考。

预期：

- `contentHash` 改变。
- 不命中旧缓存。
- 如达到摘要阈值，重新摘要并扣费。

用例 `CACHE-006`：过期清理

1. 手动将缓存 entry `createdAt` 改为 8 天前。
2. 重新选择同一参考。

预期：

- 旧缓存被删除。
- 日志出现 `AI reference summary cache expired`。
- 重新摘要并写入新缓存。

### 13.3 缓存检查方式

在浏览器控制台检查：

```js
Object.keys(localStorage).filter((key) => key.startsWith('story-ai-summary-v2'));
```

查看单条缓存：

```js
const key = Object.keys(localStorage).find((item) => item.startsWith('story-ai-summary-v2'));
JSON.parse(localStorage.getItem(key));
```

旧 key 清理检查：

```js
Object.keys(localStorage).filter((key) => key.includes('story-ai-summary') && !key.startsWith('story-ai-summary-v2'));
```

通过标准：

- 旧 key 数量为 0。
- 新 key 不包含 model 作为 key 片段。
- entry 中 `version=2`、`createdAt`、`lastUsedAt`、`summary` 存在。

## 14. AI 扣费核账专项

AI 扣费是本轮验收最高风险项。所有 AI 功能必须按“余额 + usage_logs + 页面展示 + 日志”四方核对。

### 14.1 核账公式

单次 AI 调用：

```text
余额扣减 = 调用前(member + permanent) - 调用后(member + permanent)
流水扣减 = sum(usage_logs.diamonds_consumed) by billing_group_id
断言：余额扣减 = 流水扣减
```

会员钻石与永久钻石拆分：

```text
member_diamonds before - member_diamonds after = sum(member_diamonds_used)
permanent_diamonds before - permanent_diamonds after = sum(permanent_diamonds_used)
diamonds_consumed = member_diamonds_used + permanent_diamonds_used
```

分步骤扣费：

```text
billing_group_id 相同：
  summarize = 参考摘要
  generate = 正文续写或节点生成
页面消费明细展示值 = 同 billing_group_id 下所有步骤汇总
```

缓存命中：

```text
首次长参考：summarize + generate
二次同参考：generate only
断言：第二次没有新增 summarize
```

幂等保护：

```text
同一 user_id + billing_group_id + billing_step 只能有一条有效扣费流水
```

代码层已存在唯一索引设计：

```text
idx_usage_logs_unique_billing_step(user_id, billing_group_id, billing_step)
```

验收时必须确认线上数据库已应用该迁移。

### 14.2 AI 场景覆盖矩阵

| 编号 | 场景 | 正文状态 | 参考状态 | 预期流水 |
| --- | --- | --- | --- | --- |
| BILL-001 | 正文 AI 续写 | 空正文 | 无参考 | generate |
| BILL-002 | 正文 AI 续写 | 空正文 | 短参考 | generate |
| BILL-003 | 正文 AI 续写 | 空正文 | 长参考首次 | summarize + generate |
| BILL-004 | 正文 AI 续写 | 空正文 | 长参考缓存命中 | generate |
| BILL-005 | 正文 AI 续写 | 已有正文 | 无参考 | generate |
| BILL-006 | 正文 AI 续写 | 已有正文 | 短参考 | generate |
| BILL-007 | 正文 AI 续写 | 已有正文 | 长参考首次 | summarize + generate |
| BILL-008 | 思维导图 AI 生成 | 节点已选中 | 无参考 | generate |
| BILL-009 | 思维导图 AI 生成 | 节点已选中 | 超长参考 | summarize + generate 或按实现拆分 |
| BILL-010 | 余额不足 | 任意 | 任意 | 不落库、不应用内容 |

### 14.3 失败场景断言

余额不足：

- UI 提示钻石不足。
- 不应用 AI 内容。
- 不新增章节版本。
- 不新增成功 `usage_logs`。
- 日志出现 `Budget preflight rejected` 或 `Deduct diamonds rejected by RPC`。

网络中断或扣费返回丢失：

- 如果扣费 RPC 已成功但响应丢失，应通过既有 `usage_logs` 恢复，不重复扣费。
- 日志出现 `Deduct diamonds RPC response lost but billing log was found` 或 `Deduct diamonds RPC failed after retries but billing log was found`。
- 同一 `billing_group_id + billing_step` 不得出现重复流水。

用户取消应用：

- 如果产品设计为生成完成即扣费，则扣费流水存在，但章节内容不应应用。
- 如果产品设计为应用后扣费，则取消不应扣费。
- 当前需要通过实际日志和代码路径确认最终设计，验收报告中必须写明结论。

## 15. 数据库核验脚本模板

实际执行时可用 Supabase 客户端或 SQL 查询核对。以下模板只写查询思路，不在文档中记录账号密码。

### 15.1 用户余额

```sql
select
  id,
  member_diamonds,
  permanent_diamonds,
  member_diamonds + permanent_diamonds as total_diamonds,
  membership_type,
  membership_expires_at
from profiles
where id = '<userId>';
```

### 15.2 AI 消费流水

```sql
select
  id,
  created_at,
  model_name,
  model_key,
  billing_group_id,
  billing_step,
  input_tokens,
  output_tokens,
  reasoning_tokens,
  cache_tokens,
  diamonds_consumed,
  member_diamonds_used,
  permanent_diamonds_used,
  input_diamonds,
  output_diamonds,
  reasoning_diamonds,
  cache_diamonds,
  expires_at
from usage_logs
where user_id = '<userId>'
  and created_at >= '<testStartIso>'
order by created_at desc;
```

### 15.3 账单分组汇总

```sql
select
  billing_group_id,
  array_agg(distinct billing_step) as steps,
  sum(diamonds_consumed) as total_diamonds,
  sum(member_diamonds_used) as member_used,
  sum(permanent_diamonds_used) as permanent_used,
  min(created_at) as started_at,
  max(created_at) as ended_at
from usage_logs
where user_id = '<userId>'
  and created_at >= '<testStartIso>'
group by billing_group_id
order by ended_at desc;
```

### 15.4 重复扣费检查

```sql
select
  user_id,
  billing_group_id,
  billing_step,
  count(*) as count,
  sum(diamonds_consumed) as total_diamonds
from usage_logs
where user_id = '<userId>'
  and billing_group_id is not null
  and billing_step is not null
  and created_at >= '<testStartIso>'
group by user_id, billing_group_id, billing_step
having count(*) > 1;
```

预期：返回 0 行。

### 15.5 章节与历史版本

```sql
select id, work_id, title, chapter_number, length(coalesce(content, '')) as content_len, updated_at
from chapters
where work_id = '<workId>'
order by chapter_number;
```

```sql
select id, chapter_id, source, title, length(coalesce(content, '')) as content_len, created_at
from chapter_versions
where chapter_id = '<chapterId>'
order by created_at desc;
```

### 15.6 导图内容

```sql
select id, work_id, title, type, is_default, content, updated_at
from mind_maps
where work_id = '<workId>'
order by updated_at desc;
```

核对点：

- `content.nodes` 数量变化正确。
- `content.edges` 没有悬空 source/target。
- 删除节点后没有孤儿边。

### 15.7 废稿箱

```sql
select id, type, title, original_id, deleted_at, expires_at, content
from trash_items
where user_id = '<userId>'
  and title like 'QA验收-%'
order by deleted_at desc;
```

### 15.8 指令与模板

```sql
select id, title, index, tags, source_skill_template_id, created_at, updated_at
from user_prompts
where user_id = '<userId>'
  and title like 'QA验收-%'
order by created_at desc;
```

```sql
select id, title, creator_id, category, likes, downloads, views, is_official, created_at
from community_templates
where title like 'QA验收-%'
order by created_at desc;
```

```sql
select id, title, creator_id, category, likes, uses, is_official, created_at
from community_skill_templates
where title like 'QA验收-%'
order by created_at desc;
```

### 15.9 福利

```sql
select user_id, last_check_in_date, check_in_streak, total_points_earned, completed_tasks, updated_at
from user_welfare
where user_id = '<userId>';
```

核对点：

- 签到当天只增加一次。
- `completed_tasks` 不重复写同一任务。
- 每日视频任务 key 应带日期，例如 `ad:2026-06-27`。

## 16. 页面级用例编号矩阵

本节用于后续逐项执行和记录。每一项执行后都应在最终验收报告中标记 `通过/失败/阻塞`。

### 16.1 工作台 - 思维导图

| 编号 | 功能 | 必验断言 |
| --- | --- | --- |
| MM-001 | 创建子节点 | UI 新节点、日志、`mind_maps.content` 增加节点和边 |
| MM-002 | 创建兄弟节点 | 非根成功、根节点阻止 |
| MM-003 | 编辑节点标题 | 标题保存、刷新仍存在 |
| MM-004 | 删除单节点 | 节点和边删除，根节点不可删 |
| MM-005 | 删除含子节点节点 | 子树删除数量正确 |
| MM-006 | 批量删除节点 | 多选删除正确，不误删根节点 |
| MM-007 | 撤销 | 恢复到上一步状态 |
| MM-008 | 恢复 | 撤销后可 redo |
| MM-009 | 放大 | 视图变大，不落库 |
| MM-010 | 缩小 | 视图变小，不落库 |
| MM-011 | 左对齐 | 视口变化，不落库 |
| MM-012 | 右对齐 | 视口变化，不落库 |
| MM-013 | 适应画布 | 所有节点可见 |
| MM-014 | 锁住页面 | 编辑、新增、删除被阻止 |
| MM-015 | 解锁页面 | 恢复可编辑 |
| MM-016 | 下载 | 文件生成且内容可读 |
| MM-017 | 切换主题 | 主题切换并持久化到 localStorage |
| MM-018 | AI 智能生成节点无参考 | generate 扣费，节点插入并落库 |
| MM-019 | AI 智能生成节点有参考 | 参考被使用，必要时摘要扣费 |
| MM-020 | AI 失败保护 | 失败不插入半成品节点，不扣错费 |

### 16.2 工作台 - 正文章节

| 编号 | 功能 | 必验断言 |
| --- | --- | --- |
| STORY-001 | 打开章节 | 内容从远端或本地恢复，日志可追踪 |
| STORY-002 | 选择模型 | UI 与 AI 请求模型一致 |
| STORY-003 | 参考大纲短参考 | 不摘要、不扣 summarize |
| STORY-004 | 参考大纲长参考首次 | 摘要、扣费、写缓存 |
| STORY-005 | 参考大纲缓存命中 | 不重复扣 summarize |
| STORY-006 | 无参考空正文续写 | generate 扣费，输出先展示 |
| STORY-007 | 有参考空正文续写 | 参考进入 prompt，账单正确 |
| STORY-008 | 无参考已有正文续写 | 续写结合正文上下文 |
| STORY-009 | 有参考已有正文续写 | 参考和正文均生效 |
| STORY-010 | 取消应用 | 正文不变，扣费策略写明 |
| STORY-011 | 继续应用 | 正文更新，自动保存 |
| STORY-012 | 历史版本打开 | 版本列表正确 |
| STORY-013 | 历史版本恢复 | `chapters.content` 变更，恢复前快照存在 |
| STORY-014 | 手动保存 | `chapters.content` 与 UI 一致 |
| STORY-015 | 下载 HTML/MD/TXT | 文件格式和内容正确 |
| STORY-016 | 编辑器工具栏 | 样式应用并保存 |
| STORY-017 | 网络失败保护 | 不丢失本地输入 |

### 16.3 工作台 - 我的作品导航栏

| 编号 | 功能 | 必验断言 |
| --- | --- | --- |
| NAV-001 | 新增作品 | `works` 新增，默认结构生成 |
| NAV-002 | 编辑作品名 | `works.title` 更新 |
| NAV-003 | 新增章节 | `chapters` 新增，章节号正确 |
| NAV-004 | 编辑章节名 | `chapters.title` 更新 |
| NAV-005 | 新增自定义大纲 | `mind_maps` 新增 |
| NAV-006 | 编辑导图名 | `mind_maps.title` 更新 |
| NAV-007 | 删除章节 | 移入 `trash_items`，导航消失 |
| NAV-008 | 删除导图 | 移入 `trash_items`，导航消失 |
| NAV-009 | 删除作品 | 快照完整进入废稿箱 |
| NAV-010 | 打包作品 | 下载包内容完整 |
| NAV-011 | 刷新恢复树 | 删除/新增后刷新状态一致 |

### 16.4 废稿箱

| 编号 | 功能 | 必验断言 |
| --- | --- | --- |
| TRASH-001 | 展示与筛选 | 类型、标题、时间、过期展示正确 |
| TRASH-002 | 恢复章节 | `chapters` 恢复，`trash_items` 删除 |
| TRASH-003 | 恢复导图 | `mind_maps` 恢复，内容完整 |
| TRASH-004 | 恢复作品 | `works/chapters/mind_maps` 成组恢复 |
| TRASH-005 | 恢复指令 | `user_prompts` 恢复 |
| TRASH-006 | 恢复选中 | 多项恢复部分失败不误删 |
| TRASH-007 | 删除选中 | 只彻底删除选中项 |
| TRASH-008 | 清空废稿箱 | 仅当前用户 QA 数据被清空 |
| TRASH-009 | 过期清理 | 过期项清理不影响未过期项 |

### 16.5 模版广场与社区指令

| 编号 | 功能 | 必验断言 |
| --- | --- | --- |
| COM-001 | 作品模板浏览 | 预览打开，浏览/下载指标按设计变化 |
| COM-002 | 作品模板点赞 | `template_likes` 插入/删除 |
| COM-003 | 作品模板收藏 | `user_templates` 插入/删除 |
| COM-004 | 使用作品模板 | 创建完整作品结构 |
| COM-005 | 创建作品模板 | `community_templates` 新增，creator 正确 |
| COM-006 | 编辑作品模板 | 仅本人可编辑 |
| COM-007 | 删除作品模板 | 仅本人可删除 |
| COM-008 | 提示词模板浏览 | 预览打开，指标正确 |
| COM-009 | 提示词模板复制 | 剪贴板内容正确，日志存在 |
| COM-010 | 提示词模板导入 | `user_prompts` 新增或阻止重名 |
| COM-011 | 提示词模板点赞 | `skill_template_likes` 插入/删除 |
| COM-012 | 提示词模板收藏 | `user_skill_templates` 插入/删除 |
| COM-013 | 创建提示词模板 | `community_skill_templates` 新增 |
| COM-014 | 编辑提示词模板 | 仅本人可编辑 |
| COM-015 | 删除提示词模板 | 仅本人可删除 |
| COM-016 | 我的模板 | 仅显示自己创建 |
| COM-017 | 收藏列表 | 取消收藏后即时消失 |

### 16.6 奖励中心

| 编号 | 功能 | 必验断言 |
| --- | --- | --- |
| WEL-001 | 加载福利数据 | `user_welfare` 正确读取或初始化 |
| WEL-002 | 立即签到 | 余额增加，今日不可重复 |
| WEL-003 | 去充值 | 跳转 `/membership` |
| WEL-004 | 首次 AI 生成任务 | 完成 AI 后可领取 |
| WEL-005 | 首次创建模板任务 | 创建模板后可领取 |
| WEL-006 | 观看视频任务 | 80% 后领取，不能快进 |
| WEL-007 | 重复领取保护 | 不重复加钻 |
| WEL-008 | 任务 key 一致性 | 前端任务 id 与 RPC 支持一致 |

### 16.7 使用指南

| 编号 | 功能 | 必验断言 |
| --- | --- | --- |
| GUIDE-001 | 视频教程列表 | 可打开、播放、关闭 |
| GUIDE-002 | 图文教程列表 | 可打开、内容不陈旧 |
| GUIDE-003 | 参考大纲教程 | 写明 800 字阈值、7 天浏览器缓存 |
| GUIDE-004 | AI 续写教程 | 写明先预览后应用和历史版本 |
| GUIDE-005 | 指令教程 | 写明不允许重名 |

### 16.8 指令工坊

| 编号 | 功能 | 必验断言 |
| --- | --- | --- |
| PROMPT-001 | 新增指令 | `user_prompts` 新增 |
| PROMPT-002 | 新增同名指令 | 被阻止 |
| PROMPT-003 | 编辑指令 | 内容更新，编辑自身不误判重名 |
| PROMPT-004 | 编辑为他人同名 | 被阻止 |
| PROMPT-005 | 复制指令 | 剪贴板正确 |
| PROMPT-006 | 预览指令 | 内容完整 |
| PROMPT-007 | 删除指令 | 进入废稿箱 |
| PROMPT-008 | 恢复指令 | 从废稿箱回到列表 |
| PROMPT-009 | 导入同名社区指令 | 被阻止或要求改名 |

### 16.9 消费明细

| 编号 | 功能 | 必验断言 |
| --- | --- | --- |
| RECORD-001 | 消费记录加载 | 只显示当前用户有效期内记录 |
| RECORD-002 | 消费记录聚合 | 同 `billing_group_id` 汇总正确 |
| RECORD-003 | 参考摘要流水 | summarize 可见或被聚合说明清楚 |
| RECORD-004 | AI 续写流水 | generate 可见且金额正确 |
| RECORD-005 | AI 生成节点流水 | generate 可见且金额正确 |
| RECORD-006 | 充值记录加载 | 只显示当前用户充值记录 |
| RECORD-007 | 分页 | 翻页数据不重复不遗漏 |

## 17. 已发现潜在缺陷与优化项

以下问题不是最终验收结论，但已经达到“执行前需要重点关注”的程度。

### 17.1 福利任务 ID 可能不一致

前端任务 ID：

- `first_ai_call`
- `first_template_create`
- `ad`

当前较新的 RPC 迁移中可见任务 ID：

- `first_ai_use`
- `ad`

较旧迁移中还出现过：

- `profile`
- `share`
- `ad`

风险：

- 用户点击“完成首次 AI 生成”或“完成首次创建模板”后，RPC 可能返回“任务不存在”。
- 前端展示奖励值与 RPC 实际奖励值可能不一致，例如前端首次 AI 任务展示 10000，RPC `first_ai_use` 返回 30000。

验收要求：

- 执行前先确认当前数据库实际 `claim_welfare_task` 函数支持哪些 task id。
- 前端任务 ID、文案、奖励数必须与 RPC 保持一致。
- 如果产品确实需要“首次创建模板”任务，RPC 必须增加对应校验，不能只前端展示。

### 17.2 指令新增和导入重名校验缺失

代码静态检查显示，`Prompts.tsx` 保存指令时目前只校验标题、一级标签、内容是否为空，未看到同用户标题重名阻止。

风险：

- 用户手动新增多个同名指令，列表和选择器难以区分。
- 社区导入主要按来源模板判断，可能出现不同来源但标题同名的重复指令。

验收要求：

- 新增/编辑/导入都必须做 `title.trim()` 同用户去重。
- 编辑自身不算重复。
- 导入同名必须阻止或明确要求改名。
- 建议数据库增加唯一索引兜底，应用层先做友好提示。

### 17.3 多个 UI 工具按钮缺少日志

思维导图缩放、对齐、适应画布、锁定、主题、导出等按钮，功能可能可用，但如果没有日志，本轮“结合日志验收”无法通过。

处理：

- 可把这些按钮归为轻量操作日志，成功路径一条 `log.info` 即可。
- 涉及导出、AI、删除、恢复的按钮必须有开始/成功/失败三段。

### 17.4 消费明细聚合展示需要核对分页

`Records.tsx` 查询时按原始 `usage_logs` 分页，然后前端再按 `billing_group_id` 聚合。

风险：

- 如果同一个 `billing_group_id` 的 summarize 和 generate 恰好跨分页，当前页聚合可能不完整。

验收要求：

- 构造 summarize + generate 连续记录，确认在第一页展示正确。
- 如果分页边界导致聚合不完整，应改为后端/查询层按分组分页，或一次多取后前端稳定聚合。

### 17.5 参考摘要缓存 entry 中仍保存 model 字段

当前 key 已去掉 model，但 entry 中仍记录生成摘要时的 `model`。

结论：

- 这不影响复用和扣费，是可接受的溯源字段。
- 验收时只需要确认 key 不包含 model，切换模型可命中。

### 17.6 localStorage 容量和隐私说明

参考摘要缓存存在用户浏览器，不在服务端。

风险：

- 同一台设备同一浏览器的站点数据中会保存参考摘要。
- 用户清理浏览器数据后缓存丢失。
- 换设备不会复用缓存。

建议：

- 使用指南中写清“缓存保存在当前浏览器，用于减少重复摘要扣费，7 天后自动过期”。
- 如涉及敏感创作内容，后续可增加“一键清理 AI 参考缓存”入口。

## 18. 准出标准

本轮全站登录态验收完成，必须满足以下条件才可判定通过。

### 18.1 功能准出

- 高优先级页面所有用例通过或有明确非阻塞结论。
- 作品、章节、导图、模板、指令、废稿箱核心 CRUD 均能落库并刷新保持。
- 删除类操作都有二次确认或可恢复路径。
- AI 续写、参考大纲、AI 生成节点能覆盖空正文/已有正文、有参考/无参考、缓存命中/未命中。
- 指令重名新增和导入已被阻止。
- 福利签到和任务不会重复发钻。

### 18.2 日志准出

- 高优先级按钮具备开始/成功/失败日志。
- AI 调用具备 `traceId`、`billingGroupId`、`billingStep`。
- 参考摘要缓存具备 hit/miss/write/expired/cleanup 日志。
- 删除和恢复具备业务对象 ID 与类型。
- 验收报告中每个失败项都有日志证据或明确说明“日志缺失已补/待补”。

### 18.3 数据准出

- 所有新增 QA 数据能查到。
- 所有编辑操作刷新后仍存在。
- 所有删除操作进入废稿箱或按设计彻底删除。
- 废稿箱恢复后业务表数据完整。
- 社区模板互动表插入/删除正确。
- 福利奖励更新 `profiles` 和 `user_welfare`。

### 18.4 扣费准出

- `before - after = usage_logs.diamonds_consumed 汇总`。
- `member/permanent` 拆分与流水一致。
- 参考摘要缓存命中不新增 summarize 流水。
- 同一 `billing_group_id + billing_step` 没有重复流水。
- 消费明细页面展示与数据库一致。

### 18.5 验收报告准出

最终验收报告必须包含：

- 测试环境、账号 userId、测试时间。
- 每页用例执行结果。
- 每个 AI 场景的余额与流水核账记录。
- 修复过的问题与复测结果。
- 未覆盖项及原因。
- 残余风险和上线建议。

## 19. 推荐执行节奏

为了降低互相污染，建议按以下节奏执行：

1. 准入检查与日志可用性。
2. 创建 QA 数据。
3. 先测非 AI CRUD：作品、章节保存、导图普通按钮、指令、模板。
4. 再测删除/废稿箱恢复。
5. 单独开 AI 扣费专项，逐个场景记录余额和流水。
6. 测消费明细，核对刚生成的 AI 流水。
7. 测福利奖励，避免福利加钻影响前面的 AI 核账。
8. 测使用指南和低优先充值中心。
9. 清理 QA 非流水数据，保留消费流水作为审计证据。

## 20. 第一轮执行前必须先确认的问题

以下问题需要在真正浏览器验收开始前先静态确认或快速修复：

1. 当前数据库部署的是哪个版本的 `claim_welfare_task`，是否支持 `first_ai_call` 和 `first_template_create`。
2. `Prompts.tsx` 与社区导入指令是否已经加入同用户标题重名校验。
3. 思维导图工具按钮是否已经补齐轻量操作日志。
4. 社区模板创建/编辑/删除是否已经补齐成功路径日志。
5. 消费明细按原始流水分页再聚合是否会造成跨页聚合不完整。
6. 使用指南是否已更新参考摘要 800 字阈值和 7 天浏览器缓存说明。

如果上述问题未处理，全站验收可以继续执行，但对应项必须标记为：

```text
阻塞-需修复
```

## 21. 缺陷分级与处理规则

### 21.1 缺陷等级

| 等级 | 定义 | 示例 | 处理要求 |
| --- | --- | --- | --- |
| P0 | 资金、数据安全、权限隔离、核心 AI 不可用 | 扣费重复、余额扣错、他人数据可见、AI 生成后内容丢失 | 立即停止相关验收，先修复再复测 |
| P1 | 核心功能失败或重要数据未落库 | AI 续写无法应用、废稿箱恢复失败、模板创建失败 | 本轮必须修复 |
| P2 | 功能可用但体验或日志不完整 | 工具按钮缺日志、toast 不明确、分页展示有瑕疵 | 本轮尽量修复；日志类影响验收则升为阻塞 |
| P3 | 文案、样式、低频边界问题 | 使用指南内容轻微过期、按钮 hover 状态不一致 | 可记录后排期 |

### 21.2 阻塞判定

以下情况直接判定为阻塞：

- 无法登录或登录态不稳定。
- `log/simplechat.log` 不写入。
- AI 扣费无法核账。
- 数据库查询无法确认落库。
- 删除/恢复类操作无法确认数据去向。
- 福利任务发钻与数据库不一致。
- 指令重名校验未实现但相关用例要求通过。

### 21.3 修复后复测规则

每个缺陷修复后必须复测：

1. 原失败步骤。
2. 相邻功能，不少于 2 个。
3. 数据库落库。
4. 日志完整性。
5. 如果涉及 AI，重新核账。

复测记录格式：

```text
缺陷编号：
修复文件：
修复摘要：
复测时间：
复测用例：
复测结果：
新增风险：
```

## 22. 日志检索关键字清单

执行时按页面使用以下关键字检索 `log/simplechat.log`。

### 22.1 通用

```text
Password login succeeded
Supabase client initialized
Workspace tree loaded
Data fetch useEffect
```

### 22.2 AI 与扣费

```text
Generate text started
Generate text stream started
Generate text completed
Generate text stream completed
Summarize context started
Summarize context completed
AI task billing completed
Deducting usage on server
Deduct diamonds success
Deduct diamonds skipped because billing log already exists
Budget preflight rejected
```

### 22.3 参考缓存

```text
AI reference summary cache cleanup completed
AI reference summary cache hit
AI reference summary cache expired
AI reference summary cached
AI reference pre-summary skipped
AI reference summary started
AI reference summary completed
```

### 22.4 工作台与导图

```text
Loading workspace tree
Workspace tree loaded
Saving chapter content
Chapter content saved
Saving mind map content
Mind map content saved
Loading mind map data
Loaded mind map
```

如工具按钮日志补齐后，再加入：

```text
Mind map add child
Mind map add sibling
Mind map delete
Mind map batch delete
Mind map AI generation
Mind map zoom
Mind map viewport
Mind map lock
Mind map export
Mind map theme
```

### 22.5 废稿箱、模板、指令、福利

```text
Adding item to trash
Trash item synced to Supabase
Trash items synced from Supabase
Prompt restored from trash
Creating user prompt
User prompt created
Updating user prompt
User prompt updated
Deleting user prompt
User prompt deleted
Community
Template
Skill
Welfare
fetchUsage called
fetchRecharge called
```

## 23. 人工验收与自动化分工

### 23.1 必须人工浏览验证

以下项目必须通过浏览器观察：

- 思维导图节点布局、缩放、对齐、适应画布。
- 锁定/解锁后的交互阻止。
- AI 续写流式展示、提前显示完成、继续应用体验。
- 历史版本对比与恢复。
- 模板预览、教程视频、图文教程展示。
- 激励视频不能快进与 80% 领取。
- 下载文件是否触发浏览器下载。

### 23.2 可自动化辅助核对

以下项目适合用脚本或 SQL 辅助：

- 余额前后变化。
- `usage_logs` 汇总。
- `chapters`、`mind_maps`、`trash_items`、`user_prompts`、模板表落库。
- 重复扣费查询。
- QA 数据清单。
- localStorage 缓存 key 检查。

### 23.3 不建议自动化替代的项目

- AI 内容质量主观判断。
- 模板结构是否符合创作者预期。
- 使用指南文案是否易懂。
- 导图复杂布局是否“看起来合理”。

这些可以记录人工观察结论，不作为自动化硬断言。

## 24. 页面执行记录表

后续逐页执行时，可以在本节追加实际结果。

### 24.1 汇总表

| 页面 | 用例范围 | 负责人 | 开始时间 | 结束时间 | 通过 | 失败 | 阻塞 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 工作台-思维导图 | MM-001 ~ MM-020 |  |  |  |  |  |  |  |
| 工作台-正文章节 | STORY-001 ~ STORY-017 |  |  |  |  |  |  |  |
| 我的作品导航栏 | NAV-001 ~ NAV-011 |  |  |  |  |  |  |  |
| 废稿箱 | TRASH-001 ~ TRASH-009 |  |  |  |  |  |  |  |
| 模版广场 | COM-001 ~ COM-017 |  |  |  |  |  |  |  |
| 奖励中心 | WEL-001 ~ WEL-008 |  |  |  |  |  |  |  |
| 使用指南 | GUIDE-001 ~ GUIDE-005 |  |  |  |  |  |  |  |
| 指令工坊 | PROMPT-001 ~ PROMPT-009 |  |  |  |  |  |  |  |
| 消费明细 | RECORD-001 ~ RECORD-007 |  |  |  |  |  |  |  |

### 24.2 单用例记录

```text
用例编号：
页面：
功能：
测试数据：
前置状态：
操作步骤：
UI 结果：
日志证据：
数据库证据：
余额/流水证据：
截图或录屏：
结论：
缺陷编号：
复测结果：
```

## 25. AI 核账记录表

每次 AI 调用都按一行记录。

| 编号 | 场景 | before 总余额 | after 总余额 | 余额差 | billing_group_id | steps | 流水合计 | 是否相等 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BILL-001 | 空正文无参考续写 |  |  |  |  | generate |  |  |  |
| BILL-002 | 空正文短参考续写 |  |  |  |  | generate |  |  |  |
| BILL-003 | 空正文长参考首次 |  |  |  |  | summarize + generate |  |  |  |
| BILL-004 | 空正文长参考命中缓存 |  |  |  |  | generate |  |  |  |
| BILL-005 | 已有正文无参考续写 |  |  |  |  | generate |  |  |  |
| BILL-006 | 已有正文短参考续写 |  |  |  |  | generate |  |  |  |
| BILL-007 | 已有正文长参考首次 |  |  |  |  | summarize + generate |  |  |  |
| BILL-008 | 导图 AI 无参考 |  |  |  |  | generate |  |  |  |
| BILL-009 | 导图 AI 有参考 |  |  |  |  | summarize + generate |  |  |  |

## 26. 最终验收报告模板

最终完成浏览器验收后，建议输出以下报告结构：

```text
# SimpleChat 登录态全站验收报告

## 1. 验收结论
通过 / 有条件通过 / 不通过

## 2. 测试环境
站点：
测试账号 userId：
测试时间：
浏览器：
后端日志文件：

## 3. 覆盖范围
已覆盖页面：
未覆盖页面：
未覆盖原因：

## 4. 功能结果汇总
总用例数：
通过：
失败：
阻塞：

## 5. AI 扣费核账结果
AI 场景：
余额核对：
usage_logs 核对：
消费明细核对：
重复扣费检查：

## 6. 数据落库结果
works：
chapters：
mind_maps：
chapter_versions：
trash_items：
user_prompts：
community_templates：
community_skill_templates：
user_welfare：

## 7. 日志完整性
已满足：
仍缺失：

## 8. 缺陷列表
缺陷编号：
等级：
状态：
修复建议：

## 9. 残余风险

## 10. 上线建议
```

## 27. 当前方案结论

本方案已经覆盖用户要求的登录态主要页面、功能按钮、AI 续写和参考大纲缓存、扣费流水、数据落库、日志补齐、风险识别与后续执行记录模板。

真正开始浏览器逐项验收前，建议先处理两项高风险前置问题：

1. 对齐福利中心前端任务 ID 与数据库 RPC 支持的任务 ID。
2. 实现指令工坊新增/导入的同名校验。

完成这两项后，再按第 19 节的节奏逐页验收，能最大限度减少无效返工。
