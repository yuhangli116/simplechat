# SimpleChat 安全服务方案

> 目标：防止攻击者通过高并发、刷接口、恶意输入、越权访问、资源滥用、支付绕过等方式拖垮 Node 服务、Supabase 数据库、AI 模型额度和业务资产。本文基于当前项目代码与迁移文件整理，重点覆盖 `server/index.js`、`server/aiProxy.js`、`api/ai/*`、Supabase RLS/RPC、Nginx/Vercel 部署与前端直连 Supabase 的风险。

## 1. 当前安全现状速览

### 已具备的保护

- AI 接口要求登录态：`server/aiProxy.js` 从 `Authorization: Bearer <token>` 取 Supabase access token，并用 RLS/RPC 做后续校验。
- AI 扣费已迁移到服务端：`generate`、`summarize`、`generate-stream` 统一走 `executeAiTask`，避免前端伪造 token 用量直接扣费。
- 扣费具备数据库行锁：`deduct_diamonds_v4` 对 `profiles` 使用 `FOR UPDATE`，同用户并发扣费不会把余额扣成负数。
- AI 计费有幂等键：`usage_logs(user_id, billing_group_id, billing_step)` 唯一索引可防止网络重试导致重复扣费。
- 大部分业务表已启用 RLS：`works`、`chapters`、`mind_maps`、`trash_items`、`usage_logs`、`recharge_logs`、社区互动表等都有用户隔离策略。
- 日志有本地轮转：`server/logger.js` 限制单文件 1GB、目录 5GB。
- 单次 AI 成本有上限：`AI_SINGLE_CALL_DIAMOND_CAP`，并有余额预检和异常消费提醒。

### 主要缺口

- 没有入口级并发限制：`/api/ai/generate-stream`、`/api/ai/generate`、`/api/ai/summarize` 当前没有按用户、IP、接口、全局并发做闸门。
- 没有分布式限流存储：如果部署多实例或 Vercel Serverless，多进程内存限流不可靠，需要 Redis/Upstash/Supabase RPC 兜底。
- `/api/log` 可被匿名刷写：攻击者可以持续 POST 大量日志，制造磁盘 IO 和日志膨胀。
- 支付仍是 Mock 流程：`process_membership_purchase`、`process_fuel_pack_purchase` 可由前端确认后直接把 pending 订单处理为 success，生产环境必须禁用。
- 上传缺少前端与存储层强校验：头像上传目前未限制文件类型、文件大小、路径规则和覆盖策略。
- 部署层 HTTPS、安全响应头、Nginx 限流/WAF 未完善：`deploy/nginx.conf` 当前 HTTP 配置为主，未启用 `limit_req`、`limit_conn`、安全 headers。
- 部分 Supabase RPC 是 `SECURITY DEFINER`，需要逐个限制 `GRANT EXECUTE`、固定 `search_path`、校验入参和调用者身份。
- 社区计数类 RPC 如浏览量、下载量、点赞等缺少频率约束，可能被刷榜或打数据库。

## 2. 风险分级与上线优先级

| 优先级 | 风险 | 影响 | 必须措施 |
|---|---|---|---|
| P0 | AI 接口无限并发 | Node 进程、第三方模型、Supabase 被打满，成本失控 | 应用层用户/IP/全局并发闸门 + Nginx/Vercel 限流 |
| P0 | Mock 支付可前端确认到账 | 免费刷会员/钻石，直接资产损失 | 生产禁用 Mock RPC，改为支付网关回调唯一入账 |
| P0 | Supabase RPC 权限过宽 | 越权改余额、刷福利、刷计数 | RPC 审计、显式 revoke/grant、入参和 `auth.uid()` 校验 |
| P0 | `/api/log` 匿名刷写 | 磁盘 IO、日志目录、Node 事件循环被拖垮 | 登录校验、限流、单批大小、字段截断、生产采样 |
| P1 | 上传滥用 | 存储费用、恶意文件、公开 URL 污染 | 大小/MIME/扩展名/路径策略、图片重编码 |
| P1 | 社区刷榜和写入洪泛 | 数据库写入压力、榜单污染 | 互动类 RPC 限流、唯一约束、异步聚合 |
| P1 | 缺少安全 headers/HTTPS | 中间人、XSS 后果扩大、点击劫持 | 全站 HTTPS、HSTS、CSP、Frame 防护 |
| P1 | 缺少监控告警和熔断 | 攻击发生后发现太晚 | 指标、告警、紧急开关、模型熔断 |
| P2 | 依赖与密钥治理不足 | 供应链、密钥泄露 | 依赖审计、密钥轮换、日志脱敏 |

## 3. 核心接口限流与并发方案

### 3.1 保护对象

| 接口/操作 | 风险 | 限制维度 |
|---|---|---|
| `POST /api/ai/generate-stream` | 长连接、模型成本最高、占用 Node socket | 用户并发、IP 并发、全局并发、每分钟请求数 |
| `POST /api/ai/generate` | 模型成本高 | 用户并发、IP 速率、全局速率 |
| `POST /api/ai/summarize` | 高频摘要会打模型和 DB | 用户速率、用户并发、全局速率 |
| `POST /api/log` | 匿名刷日志拖垮磁盘 | IP 速率、body 大小、批次数量 |
| Supabase 直接写入：作品、章节、导图、废稿箱 | 高频写 DB | 客户端防抖 + DB 约束 + RPC 限流 |
| 社区：创建模板、点赞、收藏、下载、浏览 | 刷榜、写入洪泛 | 用户/IP 速率、唯一约束、计数异步 |
| 头像上传 | 存储与恶意文件 | 用户频率、大小、MIME、路径 |
| Auth 登录/注册/找回密码 | 撞库、邮件轰炸 | Supabase Auth 限流 + 页面验证码/冷却 |

### 3.2 推荐阈值

先用保守值上线，观察 1-2 周后按真实用户行为调整。

| 场景 | 单用户限制 | 单 IP 限制 | 全站限制 | 失败返回 |
|---|---:|---:|---:|---|
| AI 流式生成 | 并发 1，队列 1；每分钟 3 次 | 并发 3；每分钟 10 次 | 并发 20-50，视服务器和模型额度 | `429` 或 `503` |
| AI 非流式生成 | 并发 2；每分钟 5 次 | 并发 5；每分钟 20 次 | 并发 50 | `429` |
| AI 摘要 | 并发 2；每分钟 10 次 | 并发 8；每分钟 30 次 | 并发 80 | `429` |
| 日志上报 | 每分钟 30 批，每批最多 50 条 | 每分钟 60 批 | 每分钟 3000 批 | `204` 丢弃或 `429` |
| 社区点赞/收藏 | 每分钟 30 次 | 每分钟 120 次 | 每分钟 3000 次 | `429` |
| 社区模板创建 | 每小时 10 次，每天 30 次 | 每小时 50 次 | 按 DB 承载设置 | `429` |
| 头像上传 | 每小时 10 次，单文件 2MB | 每小时 50 次 | 按存储预算设置 | `413/429` |
| 登录 | Supabase 默认 + IP 每分钟 5 次 | 每分钟 20 次 | 触发验证码 | `429` |

会员可做差异化：

- 免费用户：AI 流式并发 1，每分钟 2 次。
- 月/季/年会员：AI 流式并发 1，每分钟 5 次。
- 管理员/内测账号：白名单仅在后台配置，不写死在前端。

### 3.3 三层限流架构

#### 第一层：边缘/Nginx 粗限流

挡掉明显洪泛，减少请求进入 Node。

示例方向：

```nginx
limit_req_zone $binary_remote_addr zone=api_ip_rate:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=ai_ip_rate:10m rate=2r/s;
limit_conn_zone $binary_remote_addr zone=ip_conn:10m;

server {
  listen 443 ssl http2;
  server_name simplechat.love www.simplechat.love;

  location /api/ai/ {
    limit_req zone=ai_ip_rate burst=5 nodelay;
    limit_conn ip_conn 5;
    proxy_read_timeout 180s;
    proxy_pass http://127.0.0.1:3001;
  }

  location /api/log {
    client_max_body_size 128k;
    limit_req zone=api_ip_rate burst=20 nodelay;
    proxy_pass http://127.0.0.1:3001;
  }

  location /api/ {
    client_max_body_size 2m;
    limit_req zone=api_ip_rate burst=30 nodelay;
    proxy_pass http://127.0.0.1:3001;
  }
}
```

如果部署在 Vercel，使用 Vercel Firewall/WAF 自定义规则对 `/api/ai/*` 和 `/api/log` 设置 IP 频控、国家/ASN 黑名单、可疑 UA 阻断。

#### 第二层：Node 应用层精细限流

在 `server/index.js` 和 `api/ai/*` 入口统一调用限流中间件。必须支持：

- `key = user:<userId>:endpoint:<name>`：登录用户级限制。
- `key = ip:<ip>:endpoint:<name>`：匿名/IP 限制。
- `key = global:endpoint:<name>`：全局总量限制。
- 并发闸门：请求开始 acquire，完成/异常/连接断开 release。
- 窗口速率：滑动窗口或 token bucket。
- 返回统一错误：`429 Too Many Requests`，带 `Retry-After`。

单机 PM2 `instances: 1` 可先用进程内 Map 作为过渡，但生产建议直接用 Redis/Upstash。原因是 Vercel Serverless 和多实例 Node 的内存互不共享。

推荐模块：

```text
server/security/rateLimiter.js
server/security/concurrencyLimiter.js
server/security/requestGuards.js
```

在 AI 入口处的逻辑顺序：

1. 解析 IP、UA、Authorization。
2. 尽量从 JWT payload 取 `sub`，不信任业务权限，只用于限流 key。
3. body 大小和字段长度校验。
4. 应用 IP/用户/全局速率限制。
5. 应用 IP/用户/全局并发闸门。
6. 执行原有 `generateTextServer` / `streamGenerateTextServer` / `summarizeContextServer`。
7. `finally` 释放并发槽；监听 `req.close` 处理断连。

#### 第三层：数据库/业务兜底

即使 Node 限流失效，数据库也要能保护核心资产。

- AI 扣费：继续保留 `FOR UPDATE`、幂等唯一索引、余额不足返回。
- 福利领取：`claim_daily_checkin`、`claim_welfare_task` 必须在 RPC 内判断日期、任务唯一性和用户身份。
- 支付入账：只允许服务端支付回调函数使用 service role 或数据库受控函数入账，前端不得调用“确认支付成功”RPC。
- 社区模板：保留每用户模板数量上限，并新增每小时创建数量限制。
- 浏览/下载计数：不要让匿名/用户每次点击都直接 update 主表，可改为事件表 + 定时聚合，或给 RPC 加 `rate_limit_events` 表。

### 3.4 AI 接口特别保护

AI 生成是最容易被打穿成本的入口，建议增加：

- 输入长度硬限制：
  - `prompt` 最大 4000 字符。
  - `context` 最大 120000 字符，超过则强制摘要或拒绝。
  - `baseContentHtml` 最大 200000 字符。
- 输出长度按套餐限制：免费用户更低，会员更高。
- 高价模型开关：`claude-opus`、`gemini-3.1-pro` 等高成本模型支持后台熔断。
- 单用户小时预算：例如免费用户 1 小时最多消耗 200000 钻石，会员按套餐提高。
- 全站小时预算：例如全站 1 小时模型成本达到阈值后，只允许低价模型或暂停 AI。
- 模型供应商错误熔断：如果某 provider 连续 5 分钟 5xx/429 超过阈值，短时间内停止请求该 provider。
- 流式断连处理：客户端断开后应中止上游 fetch，避免用户断开但模型继续烧钱。
- 防重复点击：前端已有生成中状态时禁止再次触发；服务端仍以并发闸门为准。

## 4. 认证与授权

### 4.1 Supabase Auth

- 所有核心服务端接口必须要求 `Authorization: Bearer <access_token>`。
- 不要信任前端传入的 `userId`，服务端只以 JWT/Supabase `auth.uid()` 为准。
- 当前 `getAuthenticatedUser` 允许 JWT claims fallback，后续应加强：
  - 对过期 token 直接拒绝。
  - 网络异常时不要把高风险操作降级放行；AI 扣费这种资产相关操作必须经过 RLS/RPC 成功校验。
  - 记录 token 验证失败次数，触发 IP 限流。
- 前端登录、注册、找回密码页面保留 Supabase Auth 默认限流，并在 UI 增加冷却时间。

### 4.2 RLS 策略

必须保持以下原则：

- 用户私有数据只允许 `auth.uid() = user_id` 或通过所属 `works.user_id` 间接判断。
- `profiles` 不允许用户直接更新余额、会员类型、到期时间。当前已有 `billing_v4_security.sql` 删除 `Users can update own profile`，上线前确认生产库已执行。
- 社区公开数据可读，但写入必须绑定 `creator_id = auth.uid()`。
- 官方模板、定价、系统配置只能后台写入，前端 anon/authenticated 不应有写权限。
- 所有新增表必须默认启用 RLS；迁移评审时把“是否启用 RLS”作为必检项。

### 4.3 RPC 审计

对所有 `SECURITY DEFINER` 函数逐个检查：

- 是否 `set search_path = public`。
- 是否用 `auth.uid()` 判断调用者。
- 是否显式 `GRANT EXECUTE TO authenticated`，并 `REVOKE ALL FROM anon/public`。
- 是否校验入参范围、枚举值、ID 所属权。
- 是否能被前端直接调用造成资产变化。

特别关注：

- `process_membership_purchase`
- `process_fuel_pack_purchase`
- `create_recharge_order`
- `claim_daily_checkin`
- `claim_welfare_task`
- `dev_reset_welfare_state`
- `increment_template_views`
- `increment_downloads`
- `increment_skill_template_uses`
- `deduct_diamonds_v4`

生产环境必须删除或禁用 `dev_reset_welfare_state`。

## 5. 支付与资产安全

当前充值中心是模拟支付，这对开发验收可以接受，但不能生产上线。

### 5.1 生产支付规则

- 前端只能创建订单，不能确认订单成功。
- 支付成功只能来自支付网关服务端回调。
- 回调必须验签、校验金额、商品、订单号、订单状态。
- 支付回调入账必须幂等：`SELECT ... FOR UPDATE` 锁订单，只有 `pending` 能变 `success`。
- 同一第三方交易号 `transaction_id` 必须唯一。
- 回调失败后订单保持 `pending`，由补单任务主动查询支付网关状态。
- 后台人工补单必须有管理员权限、审计日志和双重确认。

### 5.2 当前必须整改

- 生产环境禁用 `process_membership_purchase` 和 `process_fuel_pack_purchase` 的前端调用。
- `payment_method = 'mock'` 的订单不得入账到生产余额。
- `Membership.tsx` 的模拟确认弹窗仅允许 `NODE_ENV !== 'production'` 或专门的测试环境。
- 充值相关 RPC 的 `GRANT EXECUTE` 按环境区分，生产只暴露创建订单和查询订单。

## 6. 数据库抗压与数据安全

### 6.1 索引与查询

重点索引：

- `usage_logs(user_id, created_at desc)`
- `usage_logs(user_id, billing_group_id, billing_step)` 唯一索引已存在，继续保留。
- `chapters(work_id, chapter_number)`
- `mind_maps(work_id, created_at)`
- `trash_items(user_id, expires_at)`
- `community_templates(creator_id, created_at)`
- `community_skill_templates(creator_id, created_at)`
- 互动表唯一索引：`template_likes(user_id, template_id)`、`user_templates(user_id, template_id)`、`skill_template_likes(user_id, skill_template_id)`、`user_skill_templates(user_id, skill_template_id)`。

### 6.2 写入削峰

- 导图和章节自动保存必须防抖、合并、去重；已有保存队列逻辑，继续保持。
- 大字段保存前比较 hash，内容没变不写库。
- 社区浏览、下载、使用次数不要每次点击都 update 主表，建议事件表 + 聚合任务。
- 日志不要把完整 prompt/context/content 写入服务端日志，只记录长度、模型、耗时、扣费、traceId。

### 6.3 数据保留

- `usage_logs` 和 `recharge_logs` 已有 TTL 思路，生产需确认保留期符合业务和财务需求。
- 业务日志保留 7-30 天即可，敏感错误脱敏。
- 删除/废稿箱保留 30 天，清理任务应分批执行，避免一次性大 delete。

## 7. 输入校验与内容安全

### 7.1 API 入参校验

建议引入统一 schema 校验，例如 Zod：

- `model` 必须在白名单。
- `billingGroupId`、`workId`、`chapterId` 必须是 UUID。
- `prompt`、`context`、`chapterTitle`、`baseContentHtml` 设置长度上限。
- `temperature` 如果未来开放，限制在 `0-1.2`。
- JSON body 超过限制直接 `413`。

### 7.2 XSS 与 HTML

- AI 输出当前会 `escapeHtml` 后转段落，继续保留。
- 富文本编辑器保存 HTML 时应白名单清洗，禁止 `<script>`、事件属性、`javascript:` URL。
- 社区模板标题、描述、标签全部当文本渲染，不使用 `dangerouslySetInnerHTML`。
- `StoryEditor` 中渲染 AI 预览 HTML 前应确保来源是已清洗内容。

### 7.3 Prompt Injection 与 LLM 滥用

小说创作场景允许用户自由输入，但仍需防滥用：

- 不把系统密钥、内部配置、隐藏提示词写入用户可见上下文。
- 不让模型输出直接触发工具调用或数据库操作。
- 对“生成 JSON 节点”的场景做结构校验，节点数、深度、字段长度都有限制。
- 对异常输出做截断和解析失败兜底，避免超大 JSON 拖垮前端。

## 8. 上传与静态资源安全

头像上传当前风险较高，建议：

- 前端限制：只允许 `image/jpeg`、`image/png`、`image/webp`，最大 2MB。
- 服务端或 Supabase Storage 策略限制：文件路径必须以 `${auth.uid()}/avatar.<ext>` 或 `${auth.uid()}-...` 开头。
- 不使用原始文件名，避免路径污染和奇怪扩展名。
- 上传后用图片处理服务重编码，剥离 EXIF。
- 禁止 SVG 头像，除非做严格清洗。
- public bucket 只放真正可公开资源，私密文件使用 signed URL。

## 9. 日志、监控与告警

### 9.1 日志安全

- `/api/log` 必须加限流和大小限制。
- 日志字段截断：`message` 500 字、`error` 2000 字、`data` JSON 10KB。
- 不记录 access token、API key、完整 prompt/context、支付回调原文中的敏感字段。
- 客户端日志生产环境采样，例如 info 10%、warn/error 100%。

### 9.2 必须监控的指标

- API：QPS、P95/P99 延迟、429 数量、5xx 数量。
- AI：每模型调用数、平均耗时、失败率、token/钻石消耗、流式断连率。
- 数据库：CPU、连接数、慢查询、锁等待、RPC 错误数。
- 业务资产：每小时新增钻石、消费钻石、充值成功金额、异常大额订单。
- 安全：登录失败次数、同 IP 多账号请求、同用户高频 AI 调用、日志上报洪峰。

### 9.3 告警阈值建议

- AI 全站 5 分钟 5xx > 5%。
- 单用户 10 分钟消耗超过日均 3 倍或超过固定阈值。
- 全站 1 小时模型消耗超过预算 50%/80%/100% 分级告警。
- Supabase 连接数超过套餐 70%。
- `/api/log` 429 持续 5 分钟，说明可能有人刷日志。
- 充值成功但无第三方交易号，立即告警。

## 10. 部署层安全基线

### 10.1 HTTPS 与 headers

生产必须：

- HTTP 全部 301 到 HTTPS。
- 开启 HSTS：`Strict-Transport-Security`。
- 禁止点击劫持：`X-Frame-Options: DENY` 或 CSP `frame-ancestors 'none'`。
- 禁止 MIME 嗅探：`X-Content-Type-Options: nosniff`。
- 控制 referrer：`Referrer-Policy: strict-origin-when-cross-origin`。
- 权限策略：`Permissions-Policy` 禁用不需要的摄像头、麦克风、定位等。
- CSP 逐步上线，先 report-only，再 enforce。

### 10.2 Node 进程

- PM2 `max_memory_restart` 保留，建议增加 `instances` 前先接入 Redis 限流。
- `express.json` body limit 保持 2MB，并针对 `/api/log` 设置更小限制。
- `app.disable('x-powered-by')` 已有，继续保留。
- 生产关闭 Vite dev proxy，避免开发配置进入正式环境。

### 10.3 密钥

- 严禁把模型 API Key 放到 `VITE_` 前缀变量。`getEnvValue` 当前会读取 `VITE_DEEPSEEK_API_KEY` 等，生产应禁止此类变量存在。
- `.env` 不入库，部署平台密钥最小权限管理。
- 定期轮换模型 API key、Supabase service role key、支付密钥。
- 任何日志和报错都不得输出密钥前 6 位以外的内容。

## 11. 应急熔断与运营后台开关

建议在 `system_config` 增加安全开关：

| key | 用途 |
|---|---|
| `ai_enabled` | 全站暂停 AI |
| `ai_stream_enabled` | 暂停流式生成 |
| `ai_allowed_models` | 动态模型白名单 |
| `ai_global_concurrency` | 全站 AI 并发上限 |
| `ai_user_concurrency_free` | 免费用户并发 |
| `ai_user_concurrency_member` | 会员并发 |
| `payment_mock_enabled` | 是否允许模拟支付，生产必须 false |
| `community_write_enabled` | 暂停社区写入 |
| `log_ingest_enabled` | 暂停前端日志上报 |

紧急处置流程：

1. 发现攻击或成本异常。
2. 开启 Nginx/Vercel 更严格限流。
3. 关闭高价模型或全站 AI。
4. 临时禁用社区写入、日志上报。
5. 拉取攻击 IP/userId/traceId，封禁或加入黑名单。
6. 复盘后调整阈值、补测试和监控。

## 12. 安全测试方案

### 12.1 并发压测

上线前至少覆盖：

- 同一用户 10 并发请求 `/api/ai/generate-stream`，预期只有 1 个执行，其他排队或 429。
- 同一 IP 50 并发请求 `/api/ai/generate`，预期触发 IP 限流，Node CPU 和内存稳定。
- 全站模拟 100 并发 AI 请求，预期全局并发闸门生效。
- 客户端断开流式连接，确认上游模型请求被中止或服务端快速释放并发槽。
- 同一 `billing_group_id + billing_step` 重试 10 次，预期只扣一次。

### 12.2 数据权限测试

- 用户 A 不能读/改/删用户 B 的 `works`、`chapters`、`mind_maps`、`trash_items`。
- 用户 A 不能通过前端传 `userId=B` 扣 B 的钻石。
- 用户不能直接 update `profiles.member_diamonds`、`membership_type`。
- 用户不能创建 `is_official=true` 的模板。
- 用户不能处理别人的充值订单。

### 12.3 资产安全测试

- Mock 支付在生产环境不可用。
- 同一订单并发回调 20 次，只入账一次。
- 金额不匹配、商品不匹配、签名错误的支付回调必须拒绝。
- 福利每日签到重复点击只到账一次。
- 任务奖励不能通过重复事件无限领取。

### 12.4 滥用测试

- `/api/log` 大 body、超多条日志、匿名刷写被拒绝或丢弃。
- 头像上传 `.html`、`.svg`、超大图片、伪造 MIME 被拒绝。
- 社区模板超长标题/描述/内容被拒绝。
- AI prompt/context 超长被拒绝或要求缩短。

## 13. 分阶段落地计划

### 第一阶段：上线前 P0

- 为 `/api/ai/*` 增加应用层用户/IP/全局并发和速率限制。
- 为 Nginx/Vercel 配置 `/api/ai/*`、`/api/log` 粗限流。
- 禁用生产 Mock 支付确认 RPC，支付入账只允许服务端回调。
- `/api/log` 增加登录/IP 限流、body 限制、字段截断。
- 确认生产库执行 `billing_v4_security.sql`，用户不能直接更新 `profiles`。
- 禁用或删除 `dev_reset_welfare_state`。
- 增加 AI 输入长度限制和高价模型熔断开关。

### 第二阶段：上线后一周

- 接入 Redis/Upstash 分布式限流。
- 增加安全监控仪表盘和告警。
- 社区计数类 RPC 加限流或异步聚合。
- 头像上传限制文件类型、大小和路径。
- 安全 headers 与 CSP report-only。
- RPC 权限审计并固化迁移。

### 第三阶段：稳定期

- 建立后台风控：封禁 IP/userId、调整模型白名单、预算熔断。
- 支付补单、退款、人工审核闭环。
- 定期依赖审计和密钥轮换。
- 建立压测脚本和安全回归用例，纳入发布流程。

## 14. 参考资料

- [OWASP API Security Top 10 2023 - API4 Unrestricted Resource Consumption](https://owasp.org/API-Security/editions/2023/en/0xa4-unrestricted-resource-consumption/)：资源消耗、限流、上传大小、第三方成本保护。
- [OWASP LLM Top 10 - Unbounded Consumption](https://genai.owasp.org/llmrisk/llm102025-unbounded-consumption/)：LLM 成本、模型调用滥用与预算熔断。
- [Supabase Row Level Security](https://supabase.com/docs/guides/database/postgres/row-level-security)：前端直连 Supabase 时的 RLS 基线。
- [Nginx limit_req module](https://nginx.org/en/docs/http/ngx_http_limit_req_module.html)：边缘层请求频率限制。
- [Vercel WAF](https://vercel.com/docs/security/vercel-waf)：Vercel 部署时的防火墙和规则配置。
