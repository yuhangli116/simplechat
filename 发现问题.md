# SimpleChat 登录态全站验收发现问题

验收时间：2026-06-27  
测试账号：1909232424@qq.com  
本地站点：http://127.0.0.1:5174  
测试批次：QA验收-20260627-登录态全站

## 验收摘要

- 登录态准入通过：账号可登录，刷新后保持登录态，工作台/废稿箱/模版广场/奖励中心/使用指南/指令工坊/充值中心/消费明细均未跳回登录页。
- 日志准入通过：`log/simplechat.log` 能持续写入前端 `/api/log` 和服务端日志。
- 主页面冒烟通过：消费明细、废稿箱、模版广场、奖励中心、使用指南、充值中心均可渲染核心内容。
- 构建验证通过：`npm run build` 通过；仅存在 Vite chunk size 和动态导入提示。

## 已确认并修复的问题

### P0 指令工坊登录用户新增指令失败

现象：在指令工坊新增 `QA验收-重复指令-20260627` 时，前端提示保存失败。  
日志证据：`Failed to create user prompt`，错误为 `invalid input syntax for type uuid: "1782574960171"`。  
原因：登录用户新增指令时把前端 `Date.now()` 临时 ID 直接写入 Supabase `user_prompts.id`，但该字段是 UUID。  
修复：`src/lib/promptPersistence.ts` 只允许合法 UUID 透传；非 UUID ID 写库前忽略，由数据库生成 ID。  
复测：重新新增同名 QA 指令成功，日志出现 `User prompt created`，数据库返回 UUID `7e000d44-dd8e-43be-af05-e739de0afbb8`。

### P1 指令工坊缺少同名校验

现象：验收前账号内已存在重复的“网文小说作家”指令，新增流程缺少标题级拦截风险。  
修复：`src/pages/Prompts.tsx` 新增/编辑保存前按 `title.trim().toLowerCase()` 检查同一用户当前指令列表，编辑自身除外。  
复测：第二次新增 `QA验收-重复指令-20260627` 被阻止，弹窗保持打开，日志出现 `Prompt duplicate blocked`。

### P1 社区提示词导入可能重复新增

现象：社区导入登录用户提示词时，创建后数据库会生成 UUID；后续再用 `community-skill-*` 本地 ID 查找会失效，可能重复导入。同名但不同来源也缺少拦截。  
修复：`src/pages/Community.tsx` 导入时优先按 `sourceSkillTemplateId` 查找，同名不同来源直接阻止；同来源允许覆盖更新。  
复测：从模版广场导入官方“网文小说作家”时被阻止，日志出现 `Skill import duplicate title blocked`。

### P1 关键业务日志不足

覆盖范围：

- 指令工坊：复制、预览、删除确认、重名拦截。
- 模版广场：作品模板/提示词模板创建、更新、删除的 requested/succeeded/failed 日志；提示词复制和导入日志。
- 奖励中心：数据加载、签到、任务跳转、任务领取、视频打开、视频进度达标、快进拦截、去充值。
- 思维导图：缩放、对齐、适应画布、撤销/恢复、锁定、主题切换、导出、AI 生成 requested/succeeded/failed。

修复文件：

- `src/pages/Prompts.tsx`
- `src/pages/Community.tsx`
- `src/pages/Welfare.tsx`
- `src/components/MindMapEditor.tsx`

复测证据：

- `Welfare recharge navigation clicked` 后跳转 `/membership`。
- `Skill import duplicate title blocked` 成功落盘。
- `Prompt duplicate blocked` 成功落盘。
- 消费明细页面触发 `fetchUsage called`。
- 废稿箱页面触发 `Syncing trash items from Supabase` / `Trash items synced from Supabase`。

## 已验证通过的页面

- 工作台思维导图：页面加载远端导图成功，节点与工具栏可见；新增/删除保存链路已有日志，工具按钮日志已补齐。
- 消费明细：页面可打开，展示最近 AI 消费记录，字段包含模型、输入/输出消耗、总消耗、会员/加油包拆分、剩余天数。
- 废稿箱：页面可打开，筛选与条目展示正常；当前废稿箱存在非 QA 历史数据，因此未执行清空操作。
- 模版广场：作品模板和提示词模板列表可打开；同名导入保护已复测。
- 奖励中心：页面可打开，余额、签到、任务和充值入口展示正常；充值入口跳转和日志复测通过。
- 使用指南：视频教程/图文指南入口页面可打开。
- 充值中心：页面可打开，会员套餐和加油包展示正常；未做真实支付验收。

## 未完全覆盖与残余风险

1. CLI 直连 Supabase 两次超时，错误为 `ConnectTimeoutError`。浏览器端 Supabase/RLS 请求正常，页面数据可加载，但命令行账实核对未完成。
2. 本轮未执行真实 AI 生成扣费复测，避免在网络不稳定时误判；建议后续单独做 AI 扣费专项，覆盖 summarize/generate/cache hit/billing_group_id。
3. 废稿箱中存在非 QA 历史数据，未执行“清空废稿箱”，只做页面展示和同步日志验证。
4. 适应画布当前会重新布局并保存节点坐标。它不改变节点文本或边关系，但与验收文档“视图操作不应改变 nodes/edges”口径存在差异，建议产品确认是否允许保存布局坐标。
5. 开发模式下部分页面会出现重复加载日志，疑似 React StrictMode/多次挂载导致；功能可用，但日志会有重复记录。

## 2026-06-28 AI 扣费专项复测

测试账号：1909232424@qq.com  
测试站点：http://127.0.0.1:5174  
测试重点：真实 AI 调用、真实扣费、消费流水、正文版本应用、摘要扣费。

### 已确认并修复的问题

#### P1 正文 AI 续写在扣费完成前显示“可应用”

现象：正文已有内容时，AI 流式输出先进入 `output_ready`，后台仍在执行扣费/保存；弹窗右侧已显示“可应用”，按钮文案为“继续应用”。如果随后扣费确认失败，会让用户误以为内容已经可以保存。  
证据：2026-06-28 00:19 左右真实续写中，模型输出成功，但 Supabase 扣费 RPC 连接超时，日志出现 `AI continue marked complete for user; background billing/save continues`，随后 `Deduct diamonds RPC failed`。  
修复：`src/pages/workspace/StoryEditor.tsx` 中版本比较弹窗只允许 `status === "ready"` 时应用；`output_ready` 改显示“结算中/结算中...”，按钮禁用。  
复测：2026-06-28 00:24 第二次真实续写成功后，只有扣费完成并返回 `ready` 后才显示“可应用/继续应用”。

#### P1 扣费幂等探测超时时未启用可重试扣费

现象：真实正文续写中，模型已经返回内容，但 `billing_idempotency_ready` 探测 Supabase 超时后，服务端没有对 `deduct_diamonds_v4` 做重试，导致本次 AI 内容无法应用。  
原因：数据库迁移已具备 `usage_logs(user_id, billing_group_id, billing_step)` 唯一索引和 `deduct_diamonds_v4` 幂等返回，但服务端在探测 RPC 超时时直接返回 `false`，未利用请求里的 `billingGroupId + billingStep`。  
修复：`server/aiProxy.js` 在幂等探测失败时，如果本次请求携带 `billingGroupId` 和 `billingStep`，启用带查重恢复的扣费重试；无幂等键时仍保持保守策略。  
复测：2026-06-28 00:24 续写中再次出现 `Billing idempotency check unavailable; fallback selected`，但 `retryEnabled=true`，随后 `Deduct diamonds success`，消费 386 钻石，余额变为 165,633,448。

### AI 场景复测结果

#### 思维导图 AI 生成

结果：通过。  
操作：在工作台思维导图选中“核心设定”，真实调用 DeepSeek V4 Flash 生成“家族秘宝 / 武盟规矩”两个子节点。  
证据：消费明细新增 2026-06-28 00:11:36 记录，总消耗 1,243 钻石；日志包含 `Mind map AI generation requested`、`AI task billing completed`、`Mind map AI generation succeeded`。  
备注：前端成功日志里的 `billingGroupId` 对无摘要场景仍可能为 `null`，服务端日志和数据库流水有真实 billing group，属于日志透传完整性问题。

#### 正文 AI 续写，无参考

结果：通过。  
操作：章节 `验收A-空正文-无参考大纲` 发起真实 AI 续写并点击“继续应用”。  
证据：消费明细新增 2026-06-28 00:24:19 记录，总消耗 386 钻石；正文从 129 字追加到 220 字；日志包含 `AI task billing completed`、`Pending AI version applied`、`Chapter version snapshot created`。

#### 正文参考大纲，短参考直用

结果：通过。  
操作：章节 `验收B-空正文-有参考大纲` 选择根节点“验收参考大纲”。  
证据：参考标签显示 1 个参考；日志 `AI reference pre-summary skipped: content below node threshold`，contentLength=119，threshold=800。  
结论：短参考不触发摘要扣费，符合当前代码策略。

#### 正文参考大纲摘要扣费

结果：通过。  
操作：通过本地 `/api/ai/summarize` 真实提交 3995 字长参考上下文，携带 `billingGroupId=94f69af4-24d3-4c93-8b03-e50964bbd78d`。  
证据：消费明细新增 2026-06-28 00:34:59 记录，总消耗 2,750 钻石；日志 `billingStep=summarize`、inputTokens=3016、outputTokens=689、reasoningTokens=553、totalRemaining=165,630,698。

#### 正文带参考 AI 续写

结果：通过。  
操作：章节 `验收B-空正文-有参考大纲` 保留 1 个参考后发起真实 AI 续写并应用。  
证据：消费明细新增 2026-06-28 00:36:02 记录，总消耗 916 钻石；正文从 154 字追加到 316 字；日志 `AI continue completed`、`Pending AI version applied`、`Chapter version snapshot created`。  
备注：服务端成功日志中 `actualDiamonds=915`，但 RPC 扣费、页面流水和返回 `total_cost` 均为 916，疑似前后端/数据库四舍五入口径有 1 钻差异；账务最终以 RPC 为准，建议后续统一日志字段口径。

### 仍需关注

1. Supabase 网络在本机 CLI/服务端偶发 `ConnectTimeoutError`，本轮通过服务端重试和幂等扣费恢复了核心链路，但 DB CLI 直查仍不稳定。
2. 参考摘要缓存复用在 StoryEditor UI 中需要足够长的真实参考节点才能完整验证；本轮已验证 summarize 真实扣费和短参考直用，未通过 UI 创建长参考节点重复验证 cache hit。
3. 消费明细目前不直接展示 `billing_step`，只能通过日志/数据库区分 summarize 与 generate；页面账单金额正确，但验收定位不够直观。

## 验证命令

```bash
npm run build
```

结果：通过。
